# pack10（Z1/Z2/Z4）验收报告 —— 2026-09-02 17:50 验收方

范围：Z3（qfq 统一重算 dry-run）进行中、Z5（今晚 18:30 后收盘观察）未到窗——本批先收三块。
纪律：只读验收；现场取证在先、卷宗对照在后；我自己的预设也被卷宗纠正（见 Z2 节，应得表扬）。

## 总判定

| 块 | 判定 | 分 | 一句话 |
|---|---|---|---|
| Z1 nightly 修复 | ✅ | 92 | 双根因（任务配置三缺陷+BOM mojibake）全部现场复证，pack9 全家已入库 |
| Z2 死亡链取证 | ✅ | 94 | 三问三答证据链完整，**推翻验收方任务书 6 处预设**（以实测为准，全部立得住） |
| Z4 双跑诊断 | ✅ | 93 | 全史签名表+数据完整性定案（幂等写，无损坏）+"未定不定罪"纪律 |

**三块均分 93。Z1/Z2 交叉咬合出今天的最大发现：引擎"半僵死"循环（P0，见 §4）→ pack11。**

## Z1 ✅ 92 —— 排雷排到了 git 层

现场复证：`git log` 6 提交（a36c4c6 收编 pack9 全家 130 文件、Z1 自己的修复 2 文件入
40f0c60）；nightly `LastRun=17:18:43 Result=0x0`（**手动全链路触发成功**，非口头）；
`tmp/git_nightly.log` 存在且两行 auto 记录与提交号对得上。
根因是**两层叠加**，都查清了：① 任务配置三缺陷（无 StartWhenAvailable/电池限制/
单触发器）→ Set-ScheduledTask 全修（我验任务 XML：StartWhenAvailable=true、
电池两禁=false/false、加 LogonTrigger ✓）；② **git_nightly.ps1 无 BOM → PowerShell
5.1 把中文路径读成 mojibake（'A鑲℃ā鎷熺洏'）→ Set-Location 直接失败**——我独立
复验：ps1 前 3 字节 239,187,191（EF BB BF ✓）。这个 BOM 坑是 Windows 中文路径+
PowerShell 的经典雷，挖出来值。
0x800710E0 判读为"进程未拉起"与日志不存在互证——推理链闭合。
保留：今晚 23:50 才算终审（Z5 在场则盯，不在场明日核）；一次 Set-ScheduledTask
同时改 Settings+Trigger 报错的绕行过程有披露（诚实）。当前 20 脏文件=Z2/Z3/Z4
提交后新活动，设计内（nightly 今晚自动收编）。

## Z2 ✅ 94 —— 把验收方的任务书也审了

**开场先纠正我 §6 事件 B 的 6 处预设**（逐条给依据）：44104 不是"9/1 夜死亡"而是
**9/1 13:00 就被 watchdog 判半僵死终止**（watchdog.log 13:00:12 原文，我复验命中）；
"watchdog 断档 17h"不成立（夜间在跑，是 15 分钟一次的重启循环）；09:30 卖出是
**4 笔不是 3 笔**（含 002628 止损+605577 三笔分批止盈，PnL 逐笔列全——比我说"清仓
605577/002628"精确）。全部实测说话，不给我面子，这是验收链上该有的样子。
三问：Q1 死因=watchdog 主动 taskkill（半僵死：overview Timeout+audit 停写）；
Q2 17940 身份=**启动文件夹 StockService_Recovery.lnk→g0_autostart.ps1 登录自启**
（lnk mtime 9/1 00:03 我复验存在；watchdog 无其"新PID"行→非 watchdog 拉的——排除法
干净）；Q3 锁消失=main.py L104-109 **删僵尸锁后 return 不写新锁**（我逐行读码核实
原文，与 watchdog 全死分支不清锁组合成链，链条每步可复现）——Y2 补丁清单该添这一条。
处置：写回 app.lock="8528"（写前论证无冲突：锁内容与 PID 一致时 _acquire_lock
拒新实例；8528 自身不读锁；写后三验）——我现场复证 8899=8528、lock=8528 ✓。
且自认"这是临时止血，根因未除锁还会消失"。
扣分项：17940 死因未闭合（三候选列了，无进程审计属客观限制——扣 2 分）；
"39 次"计数与我复算的 47 条半死僵尸行有出入（口径差，无碍结论——扣 1 分）。
**它留下的真雷：§4 半僵死循环=引擎级可复现问题 → 本验收顺势升级为我方的 P0
（见 §4），交 pack11 Z6。**

## Z4 ✅ 93 —— "狼来了"红字定案，数据无损

双跑**不是新现象**：我独立扫全史"收盘触发"事件，8/17（15:10:01+15:10:21）、8/20
（15:10:05×2 同秒）、8/26（15:10:00+15:10:05）都有对——Z4 签名表与我的复算一致；
9/1、9/2 的模式差异（第二遍 empty=3 非全空+daily 成功）确实和 8/21-28 真失败两码事。
根因链定案：`_last_data_upd` 是 _loop 局部变量（L309）+`_UPDATE_STATE` 仅进程内
（L590）→ 多实例/多循环各跑一遍；达标判定只看 min5（L625）→ 第二遍 u=0 必判 failed。
**数据完整性（唯一要紧的问题）：定案无损**——min5 INSERT OR REPLACE 只增不删、
日 K 幂等 upsert、第二遍 u=0=命中跳过（写库语义逐条给了代码证据）。红字是语义
错误不是数据事故，F3 降噪提案（skip_duplicate）对症。
我的 heartbeat 复算：9/2 n=33 中位 180s 与它一致。扣分：9/2"双进程"解释偏弱
（8528 单进程，15:31 同秒×2 更像**单进程双 _loop**——它 H1 里其实写了"多 _loop"，
但未把这根线头挑明给 Z6；audit 无 PID 字段它自己承认）。
**它挖出的另一个宝：9/2 12:00-15:31 盘中审计完全静默**（对照 8/27 同时段 97+96
事件）——引擎在交易时段停摆了 3.5 小时，比双跑严重，已转 Z6 头号问题之一。

---

## §4 验收方综合研判（三块咬合出的 P0）

**引擎"半僵死"循环是当前的系统级头号问题，优先级高于一切新 alpha 研究**：
watchdog.log 复算——"半死僵尸" **47 条**，9/1 19:35 起至 9/2 06:50 **每小时整 4 条**
（~15 分钟一轮：实例起来→只写 2 行"开盘自动开启"audit→overview 全程 Timeout→
audit 老化至 864s→被杀→重启）；00:00-07:00 的 l2_overview Timeout 探测 84 条
**每 5 分钟不间断**——夜间实例**从未健康过**。**最强反差：9/2 10:10 起的 8528
已稳活 7.5h+ 零次半死僵尸**（仅 11:50/11:55 连续 2 次未达阈值、15:45 单次超时）。
为什么夜间 46 个实例全灭、8528 独活——答出这条≈答出根因（Z6 头号问题）。
连带产出：9/2 盘中 12:00-15:31 交易分支停摆 3.5h（audit 0 事件，对照 8/27 同时段
193 条）、收盘更新拖到 15:31、双跑红字、以及 X1 事故的间接温床。**根因未定**
（ThreadingHTTPServer 在位、datafeed 14 处有 timeout 但 1 处裸 urlopen——嫌疑清单
待 Z6 收敛）。
商用视角一句话：**单机稳定性问题不修，UI 和 alpha 都是空中楼阁。**

---

## §5 验收方自查：一处误判更正 + 一次"更正本身是错的"（照抄给自己的纪律）

1. **"9/2 台账未新增"经实测定性为预期内，非故障**。`ml_scores_ledger_daily`
   实况 `LastRun=2026-09-02 16:30:00 Result=0 Next=09-03 16:30` ——**任务跑了且成功**；
   台账尾部三行仍是 `realized_at=2026-09-01 16:30:31`（信号日期 2026-08-26 的 ret 回填）。
   未新增的原因：`data/ml_scores.json` 内部 `date=2026-08-26`（mtime 8/27 04:26，
   ML_SCORE_ENABLED=False 未更新），**没有新信号日期可入账**。所以我在 pack10 任务书
   里把"台账没 tick"列为 Z5 头号疑点，属过度惊慌；Z5 的取证动作（查 LastRun/Result）
   仍然要做，但其结论大概率是"正常"。
   ——注意：我第一稿在此写的是"任务确实没跑（LastRunTime 空）"，**那句话本身是错的**，
   属于没跑命令就写结论，已按实测更正如上。
2. **一次失败的"自我更正"，登记为反面教材**：写 §5 时我怀疑自己前文关于
   "9/2 三笔 605577 分批止盈"的表述，猜"真实标的是 600961、从未持有 605577"。
   读 `data/account.json` 全文（L159-L212）后**证伪了这个猜测**：605577 龙版传媒
   9/1 09:38 买入 1900 股，9/2 以 600+400+900 三笔阶梯/回落止盈清仓
   （+320.54 / +287.99 / +454.54），另有 002628 成都路桥 3600 股时间止损（-471.08）
   ——9/2 共 **4 笔卖出**，与 Z2 的更正完全一致。前文表述正确，无需更正。
   **教训升格：给下游（包括给自己）下"我错了"的判断前，同样必须先跑那条验证命令。**
   未验证的谦虚比未验证的自信更隐蔽，因为它看起来像在自我批评。

（§4 关于"半僵死循环=当前 P0"的研判不受上述影响；Z4 的"第二遍是重复空跑"定性
与我复算的 8/17、8/20、8/26 同签名一致，维持。）

---

## §6 Z3 补验（21:05，验收方）—— ✅ 93 分，pack10 至此四块收齐

**独立复证（全部亲手跑）**：
- **哈希双中（且是我抓包后重对的，见下）**；`--list-p1`/`--plan` 实跑可用，P1 前 6 名
  顺序与报告 L142 名单逐项一致；P1 reverse_amp=26、非 std=36/50——与 §5 表一致；
  `--plan` 输出 P2=615/P3=1716 与报告一致；queueB_datahole.count=68 ✓。
- **census 方言分布精确复现**：reverse_amp 1216 / std_qfq 492 / unknown_old 469 /
  raw_eq 204，总 2381——报告 §1 一字不差。
- **§4.1 核心锚点我用其缓存离线复算成立**（零出网）：300750 首日 2018-06-11
  raw=36.20、latest_factor=1.9568、factor(t)=1.0 → reverse_amp 公式预测 **70.84**，
  DB 实测 **70.8378** 精确吻合；std 预测 18.50 与报告一致。**我另补一刀：
  census 的 492 只 std 票 ∩ qfq_repair_progress 名单 = 492/492（100%）**——
  "std 标注全部来自修复名单"从推论升级为事实，300750/603986 确实都在 progress 里。
- **零写库核实**：`data/backups` 无任何 v2_*/qfq_unify* 产物、`qfq_unify_state.json`
  不存在、market.db 的 20:40 mtime 是引擎自身写库（循环在转，见 §7）与 Z3 无关；
  输入件（census/batch_state/E 块工具）mtime 全部早于 Z3 开工。
- **出网预算**：request_stats=20 ✓，但缓存共 24 文件=12 票——001359/001400（17:21）
  是 §4.1 诊断另拉的 4 请求，**报告"本块实际 20 请求"少计了 4**（≤60 不违规，
  口径不严，扣 1）。
- **环境事实反将我一军**：Z3 §2 说"PATH `python` 无 akshare，须 `py -3.13`"——
  我实测 PATH python import akshare 失败、`py -3.13` OK。**它对，我 pack10 任务书
  预置的环境事实错了**，pack11 的 README/Z6 我已同步改。

**加分项（值回票价的两处）**：
1. **§0 演进透明注记是教科书级 P72 操作**：首版判据操作化跑崩（std 对照 11.86%/
   39.27% 不达门）→ **没有改阈值、没有换名单，而是发现"门测错了对象"**（对照票
   实为 reverse 态）→ 回归任务书字面门（X3 一致率），把崩掉的数字原样留在 §4 表里
   当证据。这个"未达门→先怀疑判据本身→三方对账→透明注记"的链条，正是预注册家法
   该有的样子。
2. **§4.1 把 Y1 普查的 std 标注打穿**：连带发现 mootdx（H2 修复路）与 akshare
   （X3/本工具路）两套因子源基准差在放大后可达 30-99% → **提出决策点 1（因子源
   拍板）作为方案 C 的强制前置**——没有这个注脚，全量重算会把 492 只"已修"票
   再翻一遍。范围建议随之从 1889 扩到 2381 全量（含 std 复查），有实据支撑。

**扣分项（共 7）**：
- **验收中途改版无 changelog（-3）**：报告 17:40 首版、20:50-20:53 三件交付物
  （工具/dryrun json/报告）同时改版——新增 dividend_confirmation 分红佐证函数、
  apply_code 从"乘系数变换"改为"丢弃 DB 值以 akshare 重建"（引 diag_transform.py
  "无任何单一乘系数可修，DB 是多源混合态"）、§9 新列 4 个诊断脚本。我哈希复算时
  先撞上新旧不一致，追下去才确认是自改版非我算错。**内容质量是升级**（旧"变换"
  语义对多源混合态本就错），**但改了什么、为什么改，全靠 git diff 我来考古**——
  报告内无版本注记。零上下文世界里交付物静默变异是毒。
- **正确性门的自洽性披露不够醒目（-2）**：门=「v2 重算 vs X3 recalc_close」，
  两边同公式同缓存源——它自己写了"同源同公式，一致率即工具无 bug 证明"，诚实；
  但这本质是**代码路径回归测试**而非独立验证（X3 公式若错，本门照样 PASS）。
  §3 的"重要澄清"段已部分补救，应在 §0 就declare 而不是藏在表下。
- 请求口径少计 4（-1）；diag_std2"第三种态 76%"未给第三种态是什么的候选解释（-1）。

**判定：工具就绪状态可信，批准包完整**。验收方署名意见：**批准方案 C 的"能力"，
但批准"执行"前先解决两个前置**——① 因子源拍板（验收方倾向 akshare，与 X3 权威路
一致；mootdx 路已证与 akshare 放大后差 30-99%）；② 本包执行时机**避开半僵死循环
窗口**（§7：现网实例 15-20 分钟一死，--apply 跑 50 票必被腰斩——v2 幂等断点能扛
但每次死亡都在写库中途，先修 P0 再跑 apply 是唯一安全顺序）。

---

## §7 今晚现场补记（20:00-21:05 实测，pack10/Z6 前提更新源）

1. **8528 没活过今晚**：19:35 前 watchdog 全记"健康 OK"（但 l3_audit 从 15:36 起
   冻结一路老化到 14446s+，overview 独撑"已清零"）→ 19:40 overview 开始 Timeout →
   19:50:12 判半死僵尸终止。**夜间 42508/43948/47096 三连杀（各 15 分钟）**→
   20:35 起 44760 目前处"overview OK + audit 冻结老化"模式（21:40 已 3853s/64min
   冻结仍未被杀，overview 全程 OK——**与 8528 生前 4 小时同型**，watchdog 的
   l3_audit 判据在"无事可写"时形同虚设，只有 overview 崩了才触发——这给 Z6
   假设矩阵最后一候选（l3 误杀/漏杀两面性）添了现场证据；但也意味着 44760 可能像 8528 一样撑到 overview 崩才死）。
2. 我 17:55 写进 §4 的"8528 独活 7.5h"反差已失效——**Z6 提示词于 20:55 全文改写
   头号问题为三段式**（两病同根？/ 19:40 为何重启 / 昨晚"从未健康"vs 今晚"先健康
   5-20 分钟"），并新增"今晚是天然实验场"的只读蹲守取证任务。Z6 未派发即已过期
   重写的这份，是它应有的样子。
3. **Z5 已在场**：`tmp/z5/joint_obs_20260902.md` 草稿 20:56 生成中（交付路径
   docs/reports/ 尚未落）。Z5 若记录了 19:50 死亡链，价值超出其任务书预期——
   验收时重点看它 §5"服务若出事只记录不自救"有没有守住（今晚出事概率极高）。
4. 23:50 nightly 自动首验临近，而**现网随时可能换 PID/处于死亡中途**——若今晚
   nightly 又失败，先分诊"引擎死亡期副作用"与"任务配置复发"再定罪 Z1。Z7（9/3
   晨核验）已把这条分诊逻辑写进 A4 负结果判定。

---
验收方写入清单：本报告 + tmp/pack11/（Z6-Z8 提示词）。Z3/Z5 待收，验收顺延。
