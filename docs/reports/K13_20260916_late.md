# K13 报告｜全部开工面：口径核对、收盘取证、盘中验收预约、缺口端点实测、eltdx 引入

- 时间: 2026-09-16 晚（收盘后全只读 + 工具/任务注册；未改 app/config，market.db 零写入）
- 任务来源: 「全部开始吧」= 昨轮待办 1+2+3+4+5

## 一、收盘更新好转（④结论第一部分，今晚实证）

- `kline` 日K 与三指数（sh000001/sz399001/sz399006）**全部追平 2026-09-16**；当日 `amount0 = 0/4983 = 0.0%`。
- **K1① 与 K2②（昨日首判未达成）今晚已成立**，连续 3 日复验收明天起算（09-17）。
- 但 audit 明细存在**关键签名**：二号段 17:11/17:29/18:04 三轮 data_update 全部 `min5_updated=0/1015, min5_rows=0, min5_empty=4~8`，amount_zero_ratio 0.85→0.69 →**靠 baostock 补额兜底拉回 0%**。**"限流自我实现"未解除**：主流程的 min5 段仍有夜间冷档/慢链风险（昨日夜间 TDX 冷档实证，见 K12 报告）。**明日观察点不变：正常节奏应 <30min 完成 09-17 主流程**。

## 二、K11 遗留 §9-2 口径核对（**分裂坐实**）

- 东财池（limitup 缓存口径，实时链条实际输入）vs eco 权威口径（K11 已统一 qg_zt_full）：
  最近 20 交易日 **zt_count 一致 1/20、差异 19/20，双向 −35~+17**。
  - 明细: `data/vendor/astock/k11_pending_sentiment_duibi.json`
- 根因（新发现，K11 报告之外）：`limitup._save` 缓存的是**拉取时刻的 EastMoney 池快照**——当日盘中缓存的是盘中瞬时数（08-20 em=36 vs eco=71 即例证，收盘后走 DB 缓存不去重拉），然后 eco 全史重算用精确板价/60bar/ST 过滤。两套口径在"当日是否已是收盘终值"上根本不同。
- **影响**：`sentiment.cached_sentiment`（trader 实盘链）→ phase 的输入在"盘后到收盘之间"有滑动窗口误差，phase 可能在日内翻转；`qg_zt_full`（回测侧）已是 eco 口径（K11 统一），**实盘/回测口径分裂坐实**。
- **修法草案（未实施，走预注册）**：sentiment 在"交易日 15:05 后 + 事件流/日K 已到 09-16 后"用 **eco 单日重算** override 东财池（source 标 eco势必保留 em 原样作对照）。开通 `SENTIMENT_POS_GATE` 前必须先合此链。
- 载体: 新增 `data/vendor/astock/k11_pending_sentiment_duibi.json` 作为材料。

## 三、盘后预约: rapid_scan 盘中验收（②待跑部分）

- **定时任务已注册**: `K13_RAPIDSCAN_ACCEPT_0917`（明日 09:45:00 一次性触发，NextRunTime 已确认）
- 流程: `tools/rapid_scan.py --rounds 30 --interval 3`（1878 宇宙）→ `tmp/k13_accept_duibi.py` 对拍 limitup 池 + 落 `docs/reports/k13_rapidscan_accept_20260917.md`；
  日志: `tmp/k13_rapid_scan.log`；脚本中 K13_PROJ env（setx 已注册）取项目路径，ps1 无中文。
- 验收判据（预注册）: C1 与 limitup 池 ±5s 对拍命中 ≥95%（首验目标 90%）；C2/C3 记录误报率初值。

## 四、缺口端点实测（③预注册切开的前置侦察）

| 端点 | 结果 |
|---|---|
| 同花顺机构一致预期 EPS `basic.10jqka.com.cn/new/{code}/worth.html` | **✓ 成功**（688017: 2026E EPS=1.05 vs 行业 1.11，16 机构，全表含年度/预测机构数/最小值/均值/最大值/行业均值） |
| 东财解禁日历 `RPT_LIFT_STAGE` | ✓ → ③备用源（含 free_date/金额/比例字段） |
| 东财股东户数 `RPT_HOLDERNUM_DET` | ✓（boundary 环比字段量纲需正常化，如 87728% 系上期极端意义） |

- 结论: **移植第一刀（一致预期 EPS → forward_pe）原料全绿**；解禁/户数端点已验证可达。
- 下一步（自动继续）：预注册 → factor 定义（fwd EPS 20/60 日高开强度/拐点）→ 小池 POC → 四窗 IC 复验。

## 五、eltdx v3.2.2 引入（⑤侦察完成，超预期）

- 安装: `pip install eltdx`（Rust 核心 wheel，CPython 3.10-3.14 cp310-abi3）。
- 今晚实测（夜间，TDX 冷档下）: `full_quotes`(五档+内外盘) **816ms/3 只**、**竞价 series 太🈶价值**（09:15 起拍全集，`unmatched_signed_raw` 带撤单方向）、**trades.history/code+date 可以点对点历史 tick**（含 `side='sell'/'buy'`）— mootdx 无这三族。
- 与你系统对接点（只出建议，不动 app）:
  1. **竞价过程序列**（09:15~09:25 逐秒）→ trader D-S5 快环"开盘即候选"的首屏原料（比 5s 五档更早）；
  2. `trades.history` → 历史 tick 补"封板承接回放"研究（3s 聚合口径，非真 L2）；
  3. `shortline_indicators()` → 封单/连板指标现成表，可对照 zt_ecosystem 温度计。
- 钉档: `data/vendor/astock/upstream/eltdx-v3.2.2-src/`（README + zip）；代理活跃期抓取成功。
- ⚠️ 引入边界: **eltdx 是"开源协议库"，数据仍 L1**——十档/撤单流的"真 L2"依旧不存在于开源，适用范围继续以 L1 为限。

## 六、产物清单

| 路径 | 说明 |
|---|---|
| `data/vendor/astock/k11_pending_sentiment_duibi.json` | ①口径对表结果（20 日，em vs eco） |
| `tmp/k11/diag1_result.json` | K11 既有逐票差异（复用确认） |
| `data/audit/audit.jsonl` | ④今晚 data_update 三轮签名（min5 0/1015, amount 靠补额） |
| `docs/reports/k13_rapidscan_accept_20260917.md` | 明日盘中验收产物（任务自动落） |
| `tools/rapid_scan.py` / `tmp/k13_accept_duibi.py` / `Documents/k13_accept_bs.ps1` | ②验收管线 |
| `data/vendor/astock/upstream/eltdx-v3.2.2-src/` | eltdx 钉档（3.2.2 Rust 核心，实测 816ms/3 只） |
| `docs/reports/k13_20260916_allhands.md` | 本文件 |

## 七、红线遵守

- 未改 `app/config.py`、未改 app 代码；market.db/min5.db 只读（`fetch_pool` 读缓存+今日池 DB 已有缓存不重拉）
- audit.jsonl 只读；服务未重启
- 新增仅：sidecar json/报告/工具脚本/定时任务（K13_RAPIDSCAN_ACCEPT_0917，一次性）
