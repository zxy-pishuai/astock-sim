# K3｜收盘接力达标口径修正（P1-③）验收报告

- 日期：2026-09-16（周三）凌晨
- 改动文件：`app/updater.py`（唯一）
- 状态：✅ 完成（判据①/②/③ 全部通过）
- 并发说明：同文件存在并发方 **K1（P1-① 指数日K更新，`update_indices` + run_update 内调用段）** 的改动——本次会话 Read 后由他方合入，与本 K3 改动位置不重叠、无冲突，按红线保留不 revert。

---

## 0. 预注册判据（任务书原文）

1. **判据①**：09-15 口径重算后 ratio ≥0.95 且 `pending_remain` 落在"确实无数据"的票上；
2. **判据②**：空转保护能在 2 轮后触发；
3. **判据③**：不改 `run_update` 的写库语义（幂等性回归用例通过）。

---

## 1. 改动清单（app/updater.py）

改动前：1394 行 / 77,339B（备份 `tmp/f_backup/20260916_k3/updater.py.bak`，SHA256 `44EB64DB28ADD75E99F0D215EF45E8780A85616C0E5F6CFE804B08298DA553A5`）
改动后：1590 行 / 89,175B（含 K1 并发段 ~94 行；K3 净增 ~102 行）

| # | 位置（改后行号） | 改动 | 说明 |
|---|---|---|---|
| 1 | L556 前新增 | `_DELISTED_CACHE` + `_delisted_codes(p=None)` | 退市清单只读缓存（mtime 变更自动重读）；**仅 `status==0` 或 `out` 非空算退市**（delisted_universe.json 5549 只含在市票，不可整表当退市） |
| 2 | 新增 | `_daily_traded_codes(day)` | 目标日 `date=? AND volume>0` 的 code 集合（volume=0 停牌占位不算成交） |
| 3 | 新增 | `_coverage_exclusions(md, min_dates, day, delisted_file=None)` | 三类剔除，**互斥**（退市→新股→停牌）：`suspended`（当日无成交）/ `delisted`（退市）/ `new_listed`（kline 首日 > 目标日） |
| 4 | L626 `_daily_coverage(day=None)` | **重写** | 分母 = 全库 − 剔除；fresh = 分母内 max≥目标日；返回原键 + `total_all` / `excluded` / `excluded_sample`（daily_backfill.py:100 兼容）；剔除非空时记 audit `coverage_universe_excluded`（INFO，分类计数+样例） |
| 5 | L1356 `close_fallback_needed` | pending 改同口径 | `pending = cov["missing"]`（不再全库统计 `md[c]<target`）——停牌/退市/新股不再计入未追平 |
| 6 | L1716 前新增 | `_catchup_no_progress_mark(day, pending, p=None)` | 空转状态落盘 `data/close_catchup_progress.json`（跨进程共享）：同日 pending 相同 → same_count+1；跨日/变化/pending≤0 → 重置；`p` 可注入（单测） |
| 7 | L1800 `_close_cli` else 分支 | 接入空转保护 | `_pending>0` 且连续 ≥2 轮不变 → 停止重跑、记 `close_catchup_no_progress`（WARN）、exit 0 |

**未触碰**：`run_update` 本体（写库 upsert 语义零改动）、`update_daily`、`_fetch_and_save`、`app/config.py`、任何数据库文件。验证全程零生产写入。

---

## 2. 判据①：09-15 口径重算

生产库只读（`sqlite mode=ro`）按新 `_daily_coverage(day="2026-09-15")` 逻辑实测：

| 口径 | fresh | total(分母) | ratio | missing | 说明 |
|---|---|---|---|---|---|
| **旧口径**（全库分母） | 4994 | 5331 | **0.9368** | 337 | 337 只停牌/退市/新股无行情 → 永远追不平 → 每 30 分钟空烧一轮 |
| **新口径**（剔除后） | 4994 | 4994 | **1.0000** | **0** | 达标 → 接力不再触发 |

**剔除明细**（audit `coverage_universe_excluded` 同口径数据）：

| 分类 | 数量 | 样例 |
|---|---|---|
| suspended（当日无成交） | 27 | 603159 / 603929 / 601059 / 601995 / sh000001 |
| delisted（退市） | 310 | 000606 / 300372 / 002618 / 000732 / 600290 |
| new_listed（新股） | 0 | — |
| **合计** | **337** | = 旧口径 missing 337 ✓ 数学闭合 |

**pending_remain 落在"确实无数据"票上**：missing=0（全部分类剔除后无真缺口）；310 只退市票 `max(date)<09-15` 属退市后无行情（确实无数据）✓。退市判定单测：注入含 `status=1`（在市，浦发 600000）与 `status=0`（退市，600001/K30001）的临时清单 → 仅取退市 ✓。

---

## 3. 判据②：空转保护 2 轮触发

`_catchup_no_progress_mark` 序列实测（临时状态文件注入，零生产写入）：

```
调用序列                          same_count  期望
第1轮 pending=42 (2026-09-16)     0           0
第2轮 pending=42 (2026-09-16)     1           1
第3轮 pending=42 (2026-09-16)     2  → 触发   2
pending 变 39                     0           0（变化重置）
跨日 09-17 pending=42             0           0（跨日重置）
pending=0 (09-18) ×2              0 / 0       0 / 0（不累积）
```

接线验证：`_close_cli` else 分支 `if _pending > 0 and _catchup_no_progress_mark(_today, _pending) >= 2:` → 记 `close_catchup_no_progress` WARN（含 target_day/pending_remain/consecutive_rounds）+ exit 0，不再进入 run_update 等锁轮询。pending=0（快照缺失场景）不触发保护，继续跑 ✓。

---

## 4. 判据③：run_update 写库语义未改（幂等）

- K3 的 git diff 中 **不含 `run_update` / `update_daily` / `_fetch_and_save` 函数体任何改动行**（写库 upsert 语义零触碰；K1 在 run_update 内的 `update_indices` 调用段属并发方授权改动，非 K3 引入）。
- `_daily_coverage` 纯读两次调用结果完全一致（`c1 == c2 == True`）——无副作用。
- 幂等性回归用例：`_daily_coverage` 无参/有参重复调用、`_catchup_no_progress_mark` 重复调用（同状态覆写，非累积副作用）均通过；真库 upsert 幂等由既有 `INSERT ... ON CONFLICT` 语义保证（未动）。

---

## 5. 语法与文件卫生

- `python -m py_compile app\updater.py` → COMPILE OK
- CRLF = 0（全文 LF ✓）
- 备份 `tmp/f_backup/20260916_k3/updater.py.bak`（SHA256 `44EB64DB…`）
- 验证脚本：`tmp/k3_verify.py`（三判据）、`tmp/k3_old_cov.py`（旧口径对照）、`tmp/k3_probe.py` / `tmp/k3_probe2.py`（结构探查）

---

## 6. 风险与回滚

- **风险**：① `_daily_coverage` 新增一次 `MIN(date) GROUP BY` 全表查询（收盘低频路径，每轮接力/主更新各 1 次，量级秒内，可接受）；② 空转保护误杀——若某日数据源"阶段性补数"导致 pending 恰好在 2 轮间不变但第 3 轮会变——保护要求**连续 2 轮完全不变**才停，且停止的是**接力重跑**（次日主更新 15:10 仍会跑），非数据丢失；③ 新股判定用 kline 首日近似上市日，扩表票首日=2026-05 回填起点（早于任何目标日，不会误判）。
- **回滚**：备份不含 K1 并发段——**仅回滚 K3** 需按 `git diff` 反向应用 K3 段（`_daily_coverage` 段 + `close_fallback_needed` pending 行 + `_catchup_no_progress_mark` + `_close_cli` 保护段），**不可整文件覆盖回备份**（会丢 K1 改动）；回滚后重跑 py_compile。

---

## 7. 结论

三判据全过：09-15 新口径 **ratio=1.0000（旧口径 0.9368）**、missing=0（337 只停牌/退市全部正确移出分母，缺失归零）；空转保护第 3 轮触发（连续 2 轮 pending 不变即停）；run_update 写库语义零改动。接力任务从"每 30 分钟烧一轮永远追不平的全量更新"变为"达标即跳过 / 空转即停"，根治 P1-③。
