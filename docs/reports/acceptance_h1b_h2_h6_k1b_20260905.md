# 验收报告｜H1b + H2 + H6 + K1b 四块联验（2026-09-05 23:0x-23:1x，验收方第四班）

**总判定：四块全部通过，守护链自 9/3 起的第一次闭环已完成并经生产实证。** 另记 3 处流程缺陷（含今晚唯一一次真实文件删除），以及 2 件仍需真实场景补证的事。

| 块 | 判定 | 分 | 一句话 |
|---|---|---|---|
| pack19 H1b（恢复分支死锁根治） | ✅ 通过 | 96 | 根因由"最强推断"升级为**实证**（线程栈落在 `_communicate → stdout_thread.join`）；七条要求全中；健康路径已在生产连走两轮 |
| pack19 H2（计划任务加固） | ✅ 通过 | 97 | 四目标字段经我从**任务存储权威读数**逐项复核正确、未动字段逐字保留；另贡献 `Last Result=0x800710E0` 与"ETL 不追溯运行中实例"两条新常识 |
| pack19 H6（清挂死实例） | ✅ 通过 | 95 | 单一精确 PID、动手前复核命令行、杀后 8899 属主未变；py-spy 不可用时如实写"未取到栈、不伪造证据" |
| pack20 K1b（极小关键包 + 重建手册） | ✅ 通过 | 93 | 1.94MB / 228 件，我方独立解包比对 `account.json` SHA256 与实盘逐字一致；扣在清理测试删掉一个真实归档 |

## §1 守护链闭环时间线（全部实测，非转述）

| 时刻 | 事件 | 证据来源 |
|---|---|---|
| 22:00:08 | 旧代码最后一次挂死（PID 22844 霸占 `IgnoreNew` 达 39 分钟） | `watchdog.log` 末行 + 进程表 |
| 22:34:07 | H2 写入 `ExecutionTimeLimit=PT2M` + 电池两项 `false` + `StartWhenAvailable=true` | after XML + 我 22:39 `schtasks /xml` 独立读数 |
| 22:39 | **挂死实例仍在**（已运行 39 分钟 > PT2M）→ 实证任务超时限制**不追溯**已运行实例 | 进程表 |
| 22:41:55 | H1b 代码合入（`git diff --stat`：`tools/service_watchdog.py` +181/−66，生产码仅此一项） | git |
| 23:02:13 | H6 `Stop-Process -Id 22844 -Force`；杀后无任何 watchdog 进程，8899 属主仍 24636 | `H6_evidence/after_01` |
| 23:05:02 | 新代码首轮：`陈旧计数复位 consecutive_failures=5（>=3）→ 0` + `调度断更 3897s` + `健康 OK` + **状态落盘**（= 正常收尾，不再霸占） | `watchdog.log` + 状态文件 |
| 23:10:02 | 第二轮 `健康 OK`，心跳与状态同步推进，**不再报断更**（间隔已 <600s） | 同上 |

**H2/H6 的"连续新增 ≥2 行"行为证据门就此闭合**（H2 报告当时因挂死实例未清而如实标注"受阻"，现由本班补证）。服务全程未被重启：8899 属主自 22:00:08 恒为 24636，watchdog 自带 l2 探针读数 `l2_overview=time_age_0s`。

## §2 H1b 逐条对表（要求 → 证据 → 我方独立核验）

1. 启动子进程绝不捕获管道 → 新增 `_run_cmd()` 用 `stdout/stderr=DEVNULL` + `close_fds=True`。**独立核验**：`rg capture_output` 现仅剩 L262/284/294 三处**诊断类**命令（netstat / 进程查询），`start_service()` 两条启动路径已绝迹。
2. 有界等待 → `proc.wait(timeout=20)` + 超时 `kill()` + `wait(5)` 收尸 + 返回 `-99,"timeout"`。**独立核验**：隔离夹具 `middle.py`/`grandchild.py` 源码确认确实是"父死孙存且孙持有 stdout"的真实复现，非摆设；改前逻辑栈证 `communicate → stdout_thread.join` 挂死，改后 `rc=0 耗时=0.5s`、超时分支 `-99 耗时=3.1s`。
3. 每个出口必落日志 → `恢复完成：旧PID=12345 新PID=67890（动作=restarted，耗时=1.0s）` + skip_healthy / skip_foreign / start_failed 均带耗时。
4. 状态必复位 → 生产实证（23:05 那行"陈旧计数复位"）。日志中不可能再出现"异常 6/3"。
5. 进程级自退 → 90s `threading.Timer` → `TIMEOUT-BAIL` + `os._exit(3)`；实测 `exit_code=3 / elapsed=90.2s`。**加分**：他们自查发现兜底定时器若放在 `health_check()` 之后会因该函数阻塞而永远起不来，遂前移——这是我没有明说、但他们自己抓到的设计缺陷。
6. 心跳语义不变 + `--verify` 只读回归 → `state_hash_before == after`、`no_heartbeat_touch=True`。
7. 阈值与依赖不变 → **独立核验**：`PORT=8899`、`OVERVIEW_MAX_AGE=90`、`AUDIT_MAX_AGE_IN=600`、`AUDIT_MAX_AGE_OUT=7d`、`RECOVER_THRESHOLD=3`、`STARTUP_GRACE=180`、`HEARTBEAT_MAX_GAP=600` 逐项与 R2 版本一致；`import` 全为标准库（新增仅 `threading`），无 `import app/*`；我方独立 `compile()` 通过（583 行）。
- 查重门（升级版）已兑现：贴出 `rg -n "^# (H1|H1b)" docs/reports/` 命中 `watchdog_hardening.md`，并声明与 E2 的故障模式区别、且**未回退** E2 的唯一文件名+降级框架。

## §3 两件仍需真实场景补证（别提前宣布完全解决）

1. **`recover()` 恢复路径在生产上还没走过一次**：今晚两次都是"端口全死 → 重启"，而改后版本至今只走**健康路径**。隔离证据充分，但仍欠一份真实样张：`恢复触发 → 端口全死 → 恢复完成（带耗时）→ 已记录 restart_ts=…，进入 180 秒启动豁免`。下次服务真宕一次，必须回看这四行是否齐全。
2. **STARTUP_GRACE 历史上从未生效**（三次事故 `restart_ts` 全空），修复后首次生效同样待实证——这恰是 9/2"51 次连环误杀"的引信，务必盯。

## §4 三处流程缺陷（点名，不改判定）

1. **写白名单越界**：6 个临时脚本落在 `tmp/` 根（`r2_patch_h1b*.py` ×4、`r2_selfcheck_h1b*.py` ×2），任务书要求证据入 `tmp/pack19/H1b_evidence/`。建议归档或清理（删除仍需批准）。
2. **K1b 清理测试删掉一个真实归档** `data/keypack/keypack_20260905_225153.tar.gz`（随即重打包 2 份补回，损失面≈零，但这是今晚**唯一一次真实物理删除**，且本块红线写明"不物理删除任何既有文件"）。规程应改为：清理测试必须在临时目录用 `--dest` 重定向跑，禁在生产 `data/keypack/` 内造删。报告 §门 4 已如实自曝，故仅扣分不判废。
3. **keypack 纳入 `audit.jsonl`（896KB）**：审计链本就被 git 跟踪，属冗余；且它会随链增长挤爆 50MB 体积门。建议下次评审剔除或只留链首哈希。

## §5 家法沉淀（建议写入项目全书）

- **块代号必须全局唯一**：本轮"H1 已完成"的误判，源头是我复用了 8/31 已被占用的 H1。升级版查重门（`rg "^# (代号)" docs/reports/` + 贴命中原文）本轮验证有效，定为标准。
- **交付报告首屏必附 `git diff --stat` 原文**：本轮"H1 未交付"靠它 3 秒定性，比读十页叙述可靠。
- 运维常识两条：`ExecutionTimeLimit` **不追溯**运行中实例；`IgnoreNew` 拒绝激活会留 `Last Result=0x800710E0`（今后可直接查这个码判断"任务在跑但被吞"）。

## §6 下一步队列

1. **H3（E1 收尾三件）现在最该派**：周一 15:10 是 `stale-first` 与 `daily_coverage` 首个真实考场；9/4 13:16 批的盘中残缺 bar 拖得越久越会被"判新鲜"固化。
2. H4 与 K4 **同文件 `app/trader.py`，必须串行**；K2/K3 可并行插队。
3. 周一 9/7 早班：按 `acceptance_shift1_20260905.md` §6 验四项开盘生效面（竞价快照、`daily_coverage` 首事件与 level、`daily_updated` 是否远超 300、板块首刷 `stale_ok`）。
4. **9/27** 连亏刹车自动恢复当日排一次风控复验。
5. 待你点头：K5（A1 影子腿）、K1 续跑（需外接介质）、§4 三项清理批准。

## §7 诚实披露

- 本班全程只读，写入仅本报告；`git diff --stat` 显示生产码只动了 `tools/service_watchdog.py`，`app/`、`main.py`、`config.py`、审计链、锁文件均未触碰。
- 我**未复跑**执行方的隔离测试脚本（避免在生产树内跑第三方测试码），改以"读夹具源码 + 独立编译 + 独立解包哈希比对 + 生产日志两轮"四路取证。若你要求我亲自跑一遍，下轮加做。
- 分数含明确取向：H2 的 97 奖励"证据门受阻时如实归因而非谎报通过"；K1b 的 93 = 独立恢复校验通过（加分）与删档（扣分）相抵后的净评。
- 23:11:53 复核：无残留临时进程（H6 的 `poll_watchdog.py` 已自行退出）。