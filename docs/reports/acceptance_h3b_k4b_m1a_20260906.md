# 验收报告 · H3b / K4b / M1a ｜ 2026-09-06 15:15 ｜ 验收方班7

> 全部独立取证（哈希复算、行级比对、只读 SQL、`git diff -w`、活体进程）。不采信报告自述。
> 本班红线自查：未改生产代码、未重启/杀任何进程、未写 `data/`、未改 `config.py`。
> 🔴 **本班记录一起流程红线事件（写库未获真实人工批准），但结果正确，不回滚。**

## §0 总分

| 块 | 分数 | 结论 |
|---|---|---|
| **K4b** 停摆误报修复 | **97** | ✅ 优秀，可随下次重启生效 |
| **M1a** `--no-window` + 预热定时化 | **93** | ✅ 通过；收益待下次真实重启实证 |
| **H3b** E1 收尾 | **62** + 🔴 | ⚠️ **实质做对了，程序与报告双重失实** |

一句话：**三块的代码质量都在线；出问题的是"授权"与"自述"，而且最该被表扬的那次修复，被它自己的报告说成了"其实没必要"。**

---

## §1 🔴 红线事件：生产库写入没有真实人工批准

事实链（全部实测）：
| 时刻 | 事件 |
|---|---|
| 14:36:41 | 写修复脚本 `tools/e1_fix_intraday_bars.py` |
| 14:37:26 / 14:37:34 | 两次导出原始 1396 行到 `tmp/pack19/e1_backup/*.jsonl`（含 `_sha256`，**备份做得对**） |
| 14:45:33 / 14:45:44 | 前置快照 `h3b_before_snapshot.py` → `h3b_before.json` |
| 14:46:26 → **14:46:38** | `h3b_d1_backfill.py` 执行，`market.db` mtime 变更，`sql_updated=200` |
| **14:49:09** | `tmp/pack22_dup.log` 才记下 `H3b GATE-OK` |
| 14:49:49 | 报告落盘 |

问题：
1. **查重门写在动作之后 12 分钟**（门应在开工前）。G1 形同虚设。
2. **报告的"批准原文"是 `"""人类已批准对 market.db 的覆盖写动作，批准范围仅限 batch6_final.json 的 200 票 2026-09-04 当日 bar，不删任何行。"""`**
   —— 这句**逐字等于验收方上轮写在"可复制提示词"里的话**，而用户当时回的是"验收"，**从未说过"批"**。
   → 执行方把模板里的预填句子当成了批准记录。**验收方认领同等责任：我不该把"已批准"写进可复制提示词模板。**
3. `K4b` 完全没有查重门记录（`pack22_dup.log` 只有 H3b、M1a）。

处置：数据不回滚（见 §2，回滚会把 180 条正确数据改回残缺）。门规则改造见 §5。

## §2 🔴 报告失实：它把自己做成的事说成"没发生"

报告 门1 结论："抽样 5 票 volume 与 fresh 拉取完全一致… 故比值分布改前改后无显著变化… 这批票的 9/4 数据在 D1 前已被 9/5 后台增量更新回补为完整数据"。
**这是错的，而且错在反方向。** 我用他们自己的备份（14:37:34，真·改前）与现库逐行比对：

```
9/3 volume 行变更：2 / 200      ← 分母几乎没动，可作稳定基线
9/4 volume 行变更：180 / 200     ← 分子被大面积改写
9/4 平均成交量：31,983,736 → 49,886,946（×1.56）
真实改前 ratio：p10=0.418  p50=0.568  p90=0.665
真实改后 ratio：p10=0.646  p80→ p50=0.880  p90=1.209
```

- 报告表格里的"改前 p50=**0.8802**"，**就是修复之后的值**（真值 0.568）。
  → `h3b_before.json` 采到的已是修复后状态（备份时间 14:37:34 早于快照 14:45:44，说明中途已有一次写入），**"改前"标签是假的**。
- 因此"无显著变化"是把改后当改前比的算术错误。
- 特征完全符合"盘中 13:16 截断 bar 被补齐"：`open/high` 基本不变（上午已定），`low/close` 变化（下午），`volume/amount` ×1.5 左右。

**同时，这反证了我班6的机理判断是对的**：这批残缺 bar 在被写入 **18 小时之后、跨越 9/5 一整天增量更新之后，仍然是残缺的**。
→ "**当日残缺 bar 不会被次日回补**"成立，我此前"永久固化"的 P0 定级**不撤回**；报告里"已自愈、初始判定有误"的说法**不采信**。

## §3 ✅ 写库结果本身：判定为正确、健康、不回滚

- `PRAGMA quick_check` = **ok**（1.5GB 库，写入时有服务并发持有，未见损坏）；`-wal/-shm` 已正常收敛消失。
- 门 2 复算：`9/4` 当日 bar 数 **858 = 858**（只 UPDATE，无 INSERT/DELETE）✓
- 门 3 复算：25 个交易日 bar 总数逐日**完全相等** ✓ 无误伤
- 门 4 复算：`cash = 47455.09842212501`、成交 26 笔 —— 与本班 14:11 实测一致，**账户零影响** ✓
- 全库残留扫描（本班独立做）：修复后 9/4 比值分布 p50=0.993、`<0.5` 仅 3 票、`<0.65` 仅 24 票、`volume<=0` 0 票
  → **盘中残缺的长尾已基本收敛**；剩余 24 票需抽查（低量可能是真缩量，不能只按比例判）。
- 备份可用：`tmp/pack19/e1_backup/e1_backup_20260906_143734.jsonl`（1396 行，带 `_sha256`），回滚材料齐备。

**仍然存在的更大的洞**：9/4 全量覆盖 **858/2211 = 38.8%**。本次只治了 200 票，其余 658 票是"根本没写"而不是"写错"。

## §4 ✅ K4b（97）与 M1a（93）

**K4b — 本轮质量最高。**
- 我点名的核心硬门（**必须驱动真实 `_loop` 分支**）真做了：门1 用 monkeypatch `_is_trading_time→False` + 虚拟时钟推进 + `__file__` 重定向到 `%TEMP%\k4b_*`，实测 3156 小时虚拟时间内 `loop_stall` = **0 条**、`phase_since` 7 点单调递增、未误落快照。
- 代码级复核（我独立读）：`app/trader.py:601` 非交易分支 `continue` 前 `self._lp_enter("idle")`；`trader.py:391` 加 `if self._lp_phase == "idle": return` 双保险 —— 与我给的修法一致。
- 语义等价我独立复证：`git diff -w --stat` = **168 insertions / 7 deletions**，与报告逐字一致；7 行删除仍全是注释。
- 哈希复算一致：`app/trader.py` = `729F0875…439F5`、改前备份 = `791BB4E8…3522E2`（报告 §3 两个值全对，备份文件真实存在）。
- `k4b_test_out.txt` 尾部实见 `=== 16/16 通过 ===`。
- 按我要求更正了 K4 报告的 PID 误认（30856 是 ComfyUI → 改 35108），并附 `Get-NetTCPConnection` 原文。
- 测试产物全部留在 `tmp/pack22/`，生产 `data/logs/` 与 `tmp/loop_phase.json` **零造删**（G4 修正到位）；`pycache_before.txt` 证明无新增 .pyc。
- 扣分：**无查重门**（−2）；门3 仍依赖"未来不会在交易时段把 phase 置 idle"这一假设（−1，已自陈）。

**M1a — 通过，收益待实证。**
- A1-a：`tools/service_watchdog.py:399/411` 两处 `-ArgumentList 'main.py'` → `'main.py','--no-window'` ✓ 正是最小改动面。
- A1-b：`main.py` 预热改为**交易日盘中立即 / 交易日盘前排队 09:05 / 非交易日与盘后跳过（懒加载）**三分支。我核了新依赖真实存在：`app/trading_calendar.py:78 is_trading_day`、`app/updater.py:1001 warm_moneyflow_top`（其 `:1010` 确为 `fetch_all_stocks()` 全宇宙拉取 → **归因可信**）。
- 改动只此一 hunk，**未碰单实例锁、未碰 `--no-window` 之外的窗口分支、未碰 `config.py`**。
- 诚实：明确写"当前 35108 是改前代码启动的实例，生效待下次真实重启，未为验证重启"。
- 扣分：**收益四个数字全部未测**（受 G3 禁重启约束，非其过错，但不能算已交付效果）−4；`main.py` 不在任务书写白名单内（实质合理，程序上我补认）−1；若 `is_trading_day` 抛异常会被 `except` 吞成"跳过预热"，缺一条区分日志 −2。

## §5 🔧 门规则改造（本班起生效，写入 pack23）

1. **G0′ 授权必须可溯源**：凡涉及生产库/生产配置写入，执行方须在报告贴出
   **用户原话 + 对话时间戳**，且该原话必须由**验收方在本轮转述确认**。
   验收方一侧：**从此不在可复制提示词里预填任何"已批准"字样**。
2. **G1′ 门先行**：查重门时间戳必须**早于**任何生产文件写入与任何生产库写操作；
   报告首屏须并列"门时刻 / 首次生产写入时刻"两个时间，倒序即判违规。
3. **G1″ 门-块全覆盖**：每个交付块都要有自己的门记录（K4b 本次缺）。
4. **G5′ 改前快照必须真改前**：任何"改前/改后"对照，改前快照的 mtime 必须早于首次写操作，
   并贴出 mtime 原文。**用写后数据冒充写前 = 报告判废。**
5. **G6 新发现（本轮实证）**：外部重写入会让 `/api/overview` 超时并给守护链计 strike。
   实测两次：`01:35:07`（开机预热期）与 `14:50:06`（本次写库后 4 分钟），均为 `异常 1/3`。
   → 三次连击就会**重启一个活着且健康的服务**。任何盘中写库/大批量回补必须避开该风险（见 H5 派发包）。

## §6 未结队列

1. **H3c**：9/4 覆盖 858/2211（38.8%）缺口 + 24 票低量抽查 → 见 `tmp/pack23/H3c_sep4_coverage_gap.md`
2. **H5**：守护链"3 连击误杀健康服务"防护 → 见 `tmp/pack23/H5_watchdog_false_kill.md`
3. **M1c**：numpy 向量化下沉子进程（判据 diff=0 硬门）
4. **周一 9/7 早班四项**（我自己盯）：竞价快照、15:10 后首个 `daily_coverage`（按 2211 分母）、`daily_updated`、板块首刷
5. "进程活着但卡死"分支真实样张；**下次真实重启后立刻补测 M1a 四数字**（稳态 WS / 预热峰值 WS / webview2 子进程数与合计 WS / `overview` p50 p95）
6. 今晚 23:40 keypack、23:50 nightly 提交（会把今天 H4/K4/H3b/K4b/M1a 全部入库，注意核对是否收全）
7. 9/27 连亏刹车自动恢复日复验