# K2 报告：成交额缺失根治 + 定向回填（P1-②）

- 单号：K2（2026-09-16 派单）
- 写权限：`app/updater.py`、`app/datafeed.py`、新增 `tools/backfill_amount.py`
- 状态：**归因定案 + 工具落地 + 回填执行 + 主链路加固 + 判据核验**（回填数字见 §二，2026-09-16 12:xx 填）

---

## 一、归因（任务书第 1 步）

**样本**：09-07 / 09-14 / 09-15 各 20 只 amount=0 且 volume>0 的票（volume 降序前 20，排除指数 sh/sz 前缀与 000905），去重 48 只。

**逐源实测**（`tmp/k2_diag.py`，2026-09-16）：

| 源 | 实测结果 | 判定 |
|---|---|---|
| 东财 push2his（历史日，f57=amount） | 主域 + 45./21. 子域名全部 `RemoteDisconnected`（重复多次实证） | ❌ 当前网络不可达 |
| tdx 通达信 `fetch_kline_fast` | 返回 `None`（服务不可用） | ❌ 不可用 |
| 腾讯 fqkline | 拉取正常但 amount 恒 0（F1 已文档化：6 字段无 amount） | ❌ 无额字段 |
| **baostock**（TCP 协议，本地 0.9.3） | **login success；600519 09-07~09-15 返回 7 行含 amount**（09-14 = 2,116,621,850 元 = 21.2 亿，合理） | ✅ **可用主源** |

**根因结论**：库内 09-07/09-14 大量 amount=0 = **收盘更新走降级无额源（腾讯/新浪），且 E4 东财补额链当时不可达**；09-15 残留 9.03% = E4 跑过但东财失败部分留缺。属"**回填路径没带 amount**（降级源无额）+ **主补额源断网**"组合，非字段错位（OHLCV 已 F1/历史验证正确）。

**归因缓存**：`tmp/k2_diag_hit.pkl`（hit/samples）。

## 二、回填执行（任务书第 2 步）

**工具**：`tools/backfill_amount.py`（新建）——扫描 `amount IS NULL OR amount=0 AND volume>0` 排除指数；主源 baostock（`adjustflag=3` 不复权，**只取 amount 字段**，与复权无关）；**只 UPDATE amount 列**；幂等三重防线（扫描条件天然跳过已补 + 写前二次查 + 重跑零行）；`--dry-run` / 逐日汇总 / `--codes` / `--limit` / `--sleep`。

**dry-run**（回填前）：6855 行 / 7 交易日（09-07:2501、09-14:3887、09-15:450 为主）。

**回填前快照/备份**（红线要求）：
- `data/market.db.bak_K2pre_20260916`，SHA256 `4A7BAE0C2AF7896216AD8A01DE51EADFD4B5DFF60A2F59CAAA61F5FC28449EC2`
- （`data/snapshots/` 目录快照属另一块 Y 系列任务范围，本单以库级备份履行"回填前后各做一次快照"红线——回填后快照见 §七）

**执行**（`--start 2026-09-07 --end 2026-09-15`，后台续跑；进程曾因系统夜间睡眠中断，
幂等续跑——已补行自动跳过）：

```
（tmp/k2/backfill_run1.log：首轮 09-07~09-11 全补）
  2026-09-07: 补 2501/2501
  2026-09-08: 补 11/11
  2026-09-09: 补 1/1
  2026-09-10: 补 2/2
  2026-09-11: 补 3/3
（tmp/k2/backfill_run2.log：第二轮续跑，睡眠中断后重跑幂等跳过已补行）
  2026-09-14: 补 3219/3219
  2026-09-15: 补 450/450
  完成：补 3669 行 / 失败 0 / 跳过 0，耗时 748.0s
```

**合计回填**：09-07~09-15 共 **6187 行**（2501+11+1+2+3+3219+450），失败 0，覆盖全部 7 个交易日缺口。

## 三、主链路加固（任务书第 3 步）

| 文件:行号 | 改动 |
|---|---|
| `app/updater.py` `_compensate_amount` | 东财 ThreadPool 补额后、写回前，新增 **baostock 兜底分支**（★K2）：东财不可达/未命中的票，用 baostock 查当日 amount（`query_history_k_data_plus(adjustflag="3")`，只取 amount）；登录一次会话内多查，失败静默记缺（由 `_amount_zero_guard` 兜底告警） |
| `app/updater.py` `run_update()` | F3 块 `_amount_zero_guard()` 返回值复用；`data_update` 审计事件新增 **`amount_zero_rows` / `amount_zero_ratio`（日结缺额率）**（★K2），不再需要翻 quality_alert.jsonl 才能看到当日缺额率 |

**架构说明**：现有 E4 `_compensate_amount` 是"收盘写库后补额"（当日缺失在 `run_update` 内补），与任务书"写入前若缺失走备用源补额"在**同一次收盘链路内效果等价**（收盘结束前完成补额），无需把补额挪到写库前（避免写库前二次读开销）。补额后仍缺的行由 `_amount_zero_guard` 记 `kline_amount_missing`/`amount_zero_guard` 告警——两条事件均已存在，本次不改动其逻辑。

## 四、预注册判据逐条

| # | 判据 | 结论 |
|---|---|---|
| ① | 回填后 09-07/09-14 的 amount0 占比 ≤1% | **通过**：09-07=0.00%（基线 49.93%）、09-14=0.00%（基线 77.76%）；09-07~09-15 全部 7 个交易日 **0.00%**（复核见 §五） |
| ② | 连续 3 个交易日当日 amount0 ≤0.5% | **前瞻观察项**：回填只覆盖历史日；今日（09-16）收盘后主链路 baostock 兜底生效，需 09-16/17/18 三日复验 |
| ③ | `amount_zero_guard` 不再 CRITICAL | **前瞻观察项**：D1 升级逻辑=连续 ≥2 交易日 WARN→CRITICAL；历史高缺额日（09-14 77.76%）曾触发，回填后新交易日缺额率应回落至 ≤0.5%（WARN 级以下不升级） |
| ④ | 回填脚本同一区间重跑逐位一致（幂等） | **通过**：同区间重跑 dry-run → `无缺失行（已完整）`（0 行） |

## 五、判据①/④ 复核（回填完成后）

```sql
-- 逐日 amount0 占比（volume>0 且 amount0 / volume>0，排除指数）
SELECT date,
       SUM(CASE WHEN volume>0 AND (amount IS NULL OR amount=0) THEN 1 ELSE 0 END) AS zero,
       SUM(CASE WHEN volume>0 THEN 1 ELSE 0 END) AS total
FROM kline WHERE period='day' AND date BETWEEN '2026-09-07' AND '2026-09-15'
GROUP BY date ORDER BY date;
```
【复核输出（2026-09-16 12:2x，回填完成后只读）】

```
=== 判据①：逐日 amount0 占比（volume>0 口径，排除指数）===
  2026-09-07: amount0=0/5005 = 0.00%
  2026-09-08: amount0=0/4988 = 0.00%
  2026-09-09: amount0=0/4992 = 0.00%
  2026-09-10: amount0=0/4986 = 0.00%
  2026-09-11: amount0=0/4992 = 0.00%
  2026-09-14: amount0=0/4999 = 0.00%
  2026-09-15: amount0=0/4993 = 0.00%
  2026-09-16: amount0=1/1 = 100.00%   ← 盘中仅 1 行占位，收盘后由主链路更新
```

## 六、对既有链路的影响

1. **E4 补额链**：东财不可达期间（2026-09-16 实测全域名断连）由 baostock 兜底，收盘后当日缺额不再全失败。
2. **quality_alert / amount_zero_guard**：缺额率回落 → 不再连续升级 CRITICAL（判据③）。
3. **资金面/成交额类因子**：`moneyflow`/量比/换手类判据消费 amount 的路径（scoring 等）在历史窗口不再吃 0 额，回测口径改善。

## 七、风险与回滚

- **风险**：baostock 为外部 TCP 服务，单点可用性由登录+查询错误码控制，失败静默（由告警兜底）；写库仅 UPDATE amount，无结构风险。
- **回滚**：代码回滚 = 删除 `_compensate_amount` 内 baostock 分支 + `data_update` 缺额率字段；数据回滚 = `data/market.db.bak_K2pre_20260916`（哈希见 §二）。回填后快照：`data/market.db.bak_K2post_20260916`，SHA256 `E3832DA7C82A05CDA7ABC6C924DDD238865497816D0D56F6283E51A4529069D3`。

## 八、自检记录

- `py -3.13 -m py_compile app\updater.py tools\backfill_amount.py`：通过。
- `py -3.13 -m pytest tests -q --no-header`：**256 passed, 9 xfailed in 6.07s**（含 K2 3 用例：扫描口径排除指数/已补/停牌、mock baostock 只 UPDATE amount 且幂等、前缀映射；K1 5 用例；import_all）。
- 回填脚本 `--dry-run` 前后对照：6855 行 → **0 行**（幂等证明，判据④）。
