# K15 执行裁定（验收方 2026-09-16 夜，补充 K15_speedup_proposals.md）

- 裁定人：验收方（本会话）；提案作者：另一 AI（预注册版原样保留，未改一字）

## 提案一：竞价哨兵（S1）—— ✅ 当场执行

- 新增 `tools/auction_sentinel.py`（只读独立工具，复用 `app/l2_auction` 客户单例/限流）
- 09:20-09:26 窗口每 10s 轮询 eltdx `auctions.series`；输出 `data/logs/auction_sentinel.jsonl`
  （d_match 撮合增量 / d_unmatch 未撮合净变化 / unmatch_dir ±方向）
- 盘后单轮验证：000993 实测 `d_match=7141, d_unmatch=-2407(卖向撤), price=16.8`，通道绿
- S1 判据（连续 3 个交易日 ≥2 次零异常）从下一交易日开始计数，达标与否三日后来对账
- 卖向撤单语义注释：signed_raw 由实测方向字段 `unmatched_direction_raw` 交叉确认

## 提案二：盘后链瘦身（S2）—— ⏸ 今晚不动，理由（可反驳）

1. S2 判据是"达标 = 前 20 日均值缩短 ≥30%"——今晚已是 20:40，若今晚即改，
   今晚夜链数据会污染基线（基线必须先干净存档一晚）。
2. K3 接力空转保护（连续 2 轮 pending 不变即停）已在现场（updater.py L1888-1901），
   报告描述的"四轮全量空跑"部分被既有保护收敛——需先读一轮**修复后**的夜链审计
   再定"剩余可瘦哪一段"，避免双改同一路径。
3. 行动：明天收盘后再审 K15-提案二，先出修复后基线，再拍板实施。

## 提案三：盯盘涨停绕缓存（S3）—— ❌ 基础事实不成立，撤案

- 实测：`TradingEngine._fast_watch_loop` 源码内 **不含 `limitup` 引用**（`inspect.getsource`
  验证 'limitup' in src == False），其涨停判定走 `eng.limit_prices(昨收)` **现算**，
  与 `limitup._save` 120s TTL 缓存无关——快环 2s 盯盘本就不吃那个缓存。
- K15 提案三描述"fast_watch 受 120s TTL 影响"系误诊（大概率把 `limitup` 面板/情绪链路
  与快环混写）；K11 口径统一课题保留，但**不是一个需要修理的案件的证据**。
- 由 S15 缓存路径的新鲜度已被我早前的 SPEED-3（开盘热窗 TTL 40s）覆盖了情绪链的时敏面。

## 本次执行清单

- `tools/auction_sentinel.py`（新，只读工具）
- 无 app/ 代码改动（提案二、三均未动）
- 冒烟 22/22（当晚最后一轮记录于 git log bf231f8）
