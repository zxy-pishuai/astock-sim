# 验收报告 · N1 / K2b ｜ 2026-09-06 19:55 ｜ 验收方班9

> 独立取证：复算连接点清单、两库 journal_mode、代码层亲自读判据行、全库量化暴露面、26 笔成交逐笔对撞。
> 红线自查：未改生产代码、未重启/杀进程、未写 `data/`（实测 `market.db` mtime 仍为 15:27:07，全天第三次写入**没有发生**）。

## §0 总分

| 块 | 分数 | 结论 |
|---|---|---|
| **K2b** `amount` 缺失归因与影响 | **97** | ✅ 本日最佳。**结论是"会污染决策"，不是"只是脏数据"** |
| **N1** SQLite 读取规范纠正 | **95** | ✅ 通过。清单逐项吻合，未改任何 .py |

两块都守住了只读约束：**`market.db` 全天停在 15:27:07，无新 `.py` 进入 M**。

---

## §1 🎯 本轮最重要结论：缺成交额会被判据当成"真缩量"，从而**生成买入信号**

### 1.1 判据链（我亲自读原文，逐行确认，非转述）
```
app/tactics.py:271   ar = np.full(n, np.nan)
app/tactics.py:273   if amount_[i - 1] > 0:
app/tactics.py:274       ar[i] = amount_[i] / amount_[i - 1]      ← amount_[i]=0 且前日>0 ⇒ ar=0.0（是实数，不是 NaN！）
app/tactics.py:664   today_ar = float(...) if not np.isnan(...) else None   ← 0.0 顺利通过，不会变 None
app/tactics.py:698   if today_ar is not None and today_ar < self.amount_ratio_lt:   ← 0.0 < 1.0 恒真
app/tactics.py:700                   → "一进二候选(首板缩量)"，且 _lb_score(..., is_a1=True)
app/tactics.py:592   _lb_score 分档 <0.7 / <1.0 / <1.5            ← ar=0 落进最高档，排名被抬高
```
**结论：`amount` 缺失 → `amount_ratio=0.0` → 被读成"极端缩量首板" → 直接产出 A1 一进二买入候选，并拿到最高分档。**
这是"**把数据缺失当成一个真实且极强的信号**"，是判据级缺陷，不是数据卫生问题。

### 1.2 暴露面（我独立全库扫描，2026 年至今）
```
2026-01-01 起 volume>0 且 amount<=0 的日K：14,136 行，涉及 146 只
其中"当日涨幅 ≥ 9.85%"（即可能构成涨停/首板）：**419 行**
样例：000004(2026-06-29/07-02/07-09/07-13)、000608(7 个涨停日)、000950(4)、000951(2)、000960(3)…
全历史该缺陷：76,100 行 / 753 只（0.8701%），与 K2b 口径一致
```
→ 我的 419 是"**上限口径**"（未叠加 `lbc==1 首板` + `非一字` + `10cm` 三道过滤）；
K2b 给的实证是"**6 票 27 个首板日全被误判**"，是"**下限口径**"。
→ **两个数都成立但差两个数量级，必须在修复块里对齐**（已写进 pack25 S1 自检门）。

### 1.3 但是：至今没有造成任何一笔真实损失（关键定级依据）
```
26 笔成交逐笔与坏行集合做 (code,date) 精确对撞 → 命中 0 笔
 traded codes 中有 2 只（000635 / 000998）落在 753 受影响集合内，但其成交日不在坏行上
```
→ 定级为 **P1（潜伏的判据缺陷，尚无已实现损失）**，而非 P0。
但它是**周一开盘就会重新暴露在真实扫描路径下的活缺陷**：9/4 当天仍有 9 只票 `volume>0/amount=0`。

## §2 K2b 为何给 97

- **10 个 `amount` 消费点逐个定性**，并区分"改变判据"与"只降低执行精度"：
  `engine.py:269` mcap=0 → 滑点回退默认（判据不变）；`factor_gtja.py` AMT×4 因子**默认未接入**
  （`config.py:235` 权重只含 VOL 因子）→ 当前不影响，启用后会。这类"现在不影响但要登记"的判断很成熟。
- **D3 读取侧修法一针见血且几乎零风险**：`amount_[i] <= 0` 时把 `ar` 置 **NaN** 而不是 0.0。
  因为**现有防护链本来就会处理 NaN**（`L664 isnan→None`、`L698 is not None` 才判、`_lb_score` 亦有 NaN 防护）
  → 改动落在数据生成处，下游一行不改，假信号自然消失。**我核对过这三道防护确实都在。**
- **反向论证扎实**：`volume>0 且 amount<=0` 物理不可能 ⇒ 写入侧拒绝不会误伤合法行；
  指数（sh000/sz399）无成交额是语义差异 ⇒ 单列豁免。
- **归因到位**：`index_timing._fetch_online` 取 `e[6]` 缺列写 0.0（A 桶根因）、
  `datafeed.py:478/507/654` 与 `updater.py:98` 的 `... or 0` 兜底（C-G 桶根因）。
  并纠正了一个我的表述：**"每日新增 9-57 票"其实不是新增，是同一批存量票每天都缺**——他说得对，我的说法不准。
- 诚实：推演"改后"时明说真实 amount 不可得，用 `close×volume` 的方向作代理并声明假设；
  000960/000969 两例**主动承认改前判定"巧合正确"**，没有把所有例子都算成自己的功劳。

扣分：暴露面口径偏窄（−2）；查重门签在 `tmp/pack24/dup.log` 而非 `tmp/pack24_dup.log`（−1）。

## §3 N1 为何给 95

- **清单我独立复算，逐项吻合**：`tools/` 下 `immutable=1` 共 **20 处 / 14 个文件**，其中 **3 个文件连的是归档快照**
  （`cyq_build`、`tactic_lianban_backtest`、`c_lane_redispatch`）→ 17 处连活库需改、3 处保留 ✓
- 两库 `journal_mode` 实测**都是 wal**（`market.db`、`min5.db`），他们报的对 ✓；
  且我实测 `-wal` 当前 size=0（已 checkpoint）→ 他们"此刻对读无差异"的说法**没有夸大**。
- 未改任何 .py ✓；把 R1/R2/R3 写成可直接粘进底稿的成稿 ✓。
- **这条要记住**：`immutable=1` 只有在"写后立即验证"或"服务正在写"的窗口才致命；
  H3c 15:27 那次正好踩中，所以不是理论风险。

扣分：`predict.py:101` 早就有注释承认踩过同一坑，N1 未顺带列出"该文件是否已修"（−2）；
"修改 17 处"整体推到下一批，但没有给出批次切分与验证顺序（−3）。

## §4 🔧 流程更正与新规

1. **更正我上轮的说法**："9/2、8/26 每天新增 55-57 票"——不是新增，是**存量票每日仍缺**。K2b 已纠正，我采纳。
2. **查重门文件名统一**（我上轮没写死，导致同一 pack 出现两种约定）：
   自 pack25 起，门**只能**写在 `tmp/packNN_dup.log`（不得写进 pack 目录内的 `dup.log`），
   行格式 `<块号> <YYYY-MM-DD HH:MM:SS> GATE-OK`。验收方审计只认这个路径。
3. **待你确认那件事仍未答**（已问两轮，不再追问，但先挂着）：H3c 声称你说过"批准"。
   若你没说过 → H3c 授权门判不通过并冻结其写权限；若说过 → 我补一条"跨会话授权须双方引用同一段原话"。
   **数据侧无需任何动作**（我已独立验证回滚干净）。

## §5 未结队列（更新）

1. **S1（新，最该先做）**：读取侧 `ar=NaN` 守卫 + 暴露面口径对齐 → `tmp/pack25/S1_amount_zero_signal_guard.md`
2. **H3d**：9/4 全量 886 票补齐（等批时间窗；N1 读规范已就位，其 17 处改动可与 S1 并行安排）
3. **N1-impl**：17 处连接点分批改造（按 N1 清单，须先给批次切分）
4. **H5-impl**：方案已可用（P1+P2），待 `tools/service_watchdog.py` 脱离 M
5. **M1c**；6. **周一 9/7 早班四项**（我自盯）；7. 今晚 23:40 keypack / 23:50 nightly；8. 9/27 风控复验。