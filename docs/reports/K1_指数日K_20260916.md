# K1 报告：指数日K接入收盘主更新（P1-①）

- 单号：K1（2026-09-16 派单）
- 写权限：`app/updater.py`、`app/datafeed.py`
- 状态：**代码已落地 + 真实执行成功 + 判据①②③④⑤ 逐条核验（②为前瞻观察项）**

---

## 一、改动清单

| 文件 | 位置 | 改动 |
|---|---|---|
| `app/datafeed.py` | 新增 `fetch_index_kline(code, days=600)` | 腾讯 fqkline 拉取，按 F1（2026-09-08）确立的 6 字段映射 `[date, open, close, high, low, volume]` 解析为 9 元组 `(date, open, high, low, close, volume, amount=0.0)`；过 `_sane_rows` 行断言；失败返回 `[]`。**amount 显式写 0**（腾讯 fqkline 无 amount，指数成交额需另接源——F1 继承局限） |
| `app/updater.py` | 新增 `update_indices(verbose=False, days=600)` | `INSERT OR REPLACE` 幂等 upsert，单连接单事务，与个股同表 `kline WHERE period='day'`；逐指数失败 → `audit index_daily_update_failed` ERROR（含 error 文本，**无 except:pass**）；成功 → `audit index_daily_updated` INFO（n_codes/max_date/backfilled/per_code） |
| `app/updater.py` | `run_update()` 接线 | 在 A3 同进程续跑块之后、`_amount_zero_guard`（F3）之前插入 `update_indices(verbose=verbose)`（★K1 标记），收盘主链路每日自动补更指数 |
| `tests/test_k1_indices.py` | 新建（5 用例） | J4 冒烟：假 HTTP（patch `datafeed._http`）驱动真实解析+写库链路，断言 H≥max(O,C) 且 L≤min(O,C)、幂等、audit 事件、坏 bar 拒写、拉空→failed+ERROR |

覆盖范围：`datafeed.INDEX_CODES = {sh000001, sz399001, sz399006}`（三指数；grep 全项目引用清单见 §五）。
`000905`（中证500）仅被 `tools/data_sentinel.py` 告警引用、不入 `kline` 回填（不属指数日K主链路，报告说明）。

## 二、真实执行与前后对比

**执行前基线**（2026-09-16 只读查询）：三指数 MAX(date)=2026-09-11（各 1918 行）；个股 MAX(date)=2026-09-15。

**备份**：`data/market.db.bak_K1_20260916`（1,976,619,008 B，SHA256 `07CC7023A3E6CA69125EAAC161043E5A97A6E2F1ABDC9F93EB7F11EA51BB9320`）。

**执行**（`update_indices(verbose=True)`）：

```
指数 sh000001: 写 600 根（库内 1920 行，回填 2 根，最新 2026-09-15）
指数 sz399001: 写 600 根（库内 1920 行，回填 2 根，最新 2026-09-15）
指数 sz399006: 写 600 根（库内 1920 行，回填 2 根，最新 2026-09-15）
RESULT: {"updated": 3, "failed": 0, "max_date": "2026-09-15", "backfilled": 6, ...}
```

**执行后**（只读复核）：

```
sh000001: MAX=2026-09-15 行=1920 字段违例=0
sz399001: MAX=2026-09-15 行=1920 字段违例=0
sz399006: MAX=2026-09-15 行=1920 字段违例=0
个股 MAX(date): 2026-09-15
```

缺口回填 6 根（09-14/09-15 × 3 指数）。**三大指数 MAX(date) 已与个股对齐 = 2026-09-15**（判据①达成）。

**落库抽样**（与真实行情一致）：

| 指数 | 日期 | O | H | L | C |
|---|---|---|---|---|---|
| sh000001 | 09-14 | 3867.02 | 3895.51 | 3867.02 | 3885.33 |
| sh000001 | 09-15 | 3879.73 | 3891.62 | 3858.15 | 3864.28 |
| sz399001 | 09-15 | 13362.17 | 13484.0 | 13273.31 | 13287.97 |
| sz399006 | 09-15 | 3280.85 | 3315.33 | 3245.06 | 3247.92 |

**audit 事件**（`data/audit/audit.jsonl`）：

```json
{"t": "2026-09-16 00:12:59", "kind": "daily", "event": "index_daily_updated",
 "level": "INFO", "n_codes": 3, "max_date": "2026-09-15", "backfilled": 6,
 "failed": 0, "per_code": {"sh000001": 1920, "sz399001": 1920, "sz399006": 1920},
 "hash": "795c270d34dbacae", "prev": "b2e327c472aa9674"}
```

## 三、预注册判据逐条

| # | 判据 | 结论 |
|---|---|---|
| ① | 三指数 `MAX(date)` == 个股 `MAX(date)` | **通过**：均 = 2026-09-15（复核 SQL 见 §二） |
| ② | 连续 3 个交易日复验不回退 | **前瞻观察项**：今日（09-16）首次落地，需 09-16/09-17/09-18 三个收盘日复验；已接入 `run_update()` 每日自动补更，回退风险=收盘链路故障 |
| ③ | 新增 audit `index_daily_updated`（n_codes/max_date/缺口回填条数） | **通过**：事件已落库（见 §二），含全部字段；逐指数失败另有 `index_daily_update_failed` ERROR |
| ④ | 失败必须 `audit.record(level='ERROR')`，禁止 `except Exception: pass` | **通过**：`update_indices` 内逐指数 try/except 显式记 ERROR 并携带 error 文本；不存在裸 except |
| ⑤ | J4 冒烟：假 HTTP 断言写库行数与字段关系 H≥max(O,C)≥L | **通过**：`tests/test_k1_indices.py` 5 用例全绿（patch `datafeed._http` 走真实解析+写库链路；`_sane_rows` 拒 H<L 坏 bar 有 WARN 实证）；与 import_all 同跑 `7 passed in 1.64s` |

## 四、对既有判据/链路的影响

1. **R2 指数择时（`app/engine.py`）**：此前用 `SELECT date, close WHERE code='sh000001'`——F1 修复后 close 已正确（不再是最低价错位），本次把指数数据源追平到当日，R2 判定"站上 20 日线"使用当日 close，不再因数据滞后 2 个交易日而偏严。
2. **`app/index_timing.py` 三层因子**：直接消费 kline 指数行，数据新鲜后因子不再因缺最新日而失真。
3. **索引健康检查 / 快照（`data_sentinel` 等）**：三指数新鲜度提升，`idx_stale` 类告警（若有）应消失；`000905` 不在本次范围（仅告警用，见 §五）。

## 五、指数代码清单（grep 全项目引用）

| 代码 | 引用方 | K1 是否覆盖 |
|---|---|---|
| sh000001 | datafeed.INDEX_CODES / engine R2 / index_timing / server 健康检查 / fetch_index_kline / data_sentinel | ✅ 是 |
| sz399001 | datafeed.INDEX_CODES / server 健康检查 / fetch_index_kline | ✅ 是 |
| sz399006 | datafeed.INDEX_CODES / server 健康检查 / fetch_index_kline | ✅ 是 |
| 000905 | data_sentinel（仅告警） | ❌ 否——不属指数日K主链路，报告说明即可 |

## 六、已知局限（F1 继承）

- **指数 amount 恒为 0**：腾讯 fqkline 6 字段无 amount（F1 文档化），`fetch_index_kline` 显式写 0；指数成交额需另接源（东财指数行情接口），不在本单范围。
- 指数更新窗口 days=600（约 2.4 年），与个股全史深度不同——指数 1920 行已覆盖 2018-10-22 起，够 R2/index_timing 使用；如需更深历史另开单。

## 七、风险与回滚

- **风险**：`update_indices` 每收盘日多 3 次腾讯请求（指数），失败静默记 ERROR 不影响主流程；写库与个股同事务风格（幂等 upsert）。
- **回滚**：若需回退代码，删除 `update_indices` 函数与其在 `run_update()` 的调用 + `fetch_index_kline` 即可（数据行不受影响）；数据回滚用 `data/market.db.bak_K1_20260916`（哈希见 §二）。

## 八、自检记录

- `py -3.13 -m py_compile app\updater.py app\datafeed.py`：通过。
- `py -3.13 -m pytest tests\test_k1_indices.py tests\test_import_all.py -q`：**7 passed in 1.64s**。
- 真实执行后只读复核：三指数 MAX=2026-09-15、字段违例 0（H≥max(O,C)、L≤min(O,C) 全过）。
