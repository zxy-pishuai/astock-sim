# W1 交付报告 · git 仓库化（2026-09-01）

> 任务：把 `C:\Users\26838\A股模拟盘` 纳入 git 本地版本控制（纯本地、禁网络），终结"考古式验收"。
> 执行模式：完全访问；红线全部遵守：**无 push/无 remote/无 fetch**；**未改动 app/、tools/ 既有代码与任何报告正文**（仅新增文件与追加一节）；**基线提交对象总量 4.68 MiB < 80MB**；**只提交一次基线**。

## 0. 查重门
- `Test-Path .git` → **不存在** → 全程执行模式。
- git 2.55.0.windows.2 可用；服务在线 8899（PID 28260，未受影响）。

## 1. 目录尺寸实测与 ignore 决策（du 实测，2026-09-01 00:10）
| 路径 | 实测尺寸 | 决策 | 理由 |
|---|---|---|---|
| `data/market.db` | 1241.6 MB | 排除 | GB 级二进制，文件层备份为准 |
| `data/min5.db` | 699.6 MB | 排除 | 同上 |
| `data/snapshots/` | 4247.2 MB | 排除 | 同上 |
| `data/backups/` | 1983.2 MB | 排除 | 同上 |
| **`data/market.db.bak`** | **64.5 MB** | 排除 | 意外发现的备份，`data/market.db*` 通配符一并覆盖 |
| **`data/market.db.bak.20260819_011241`** | **64.9 MB** | 排除 | 同上 |
| `tools/ml_sidecar/.venv/` | 306.6 MB | 排除 | 虚拟环境，可再生 |
| `data/*.json` 合计 | 34.3 MB | 跟踪 | 文本小件全跟（bt_*.json、台账、triage 等） |
| `data/audit/` (audit.jsonl) | 0.6 MB | 跟踪 | 审计链存证 |
| `tmp/` 全层 | 0.9 MB | 部分跟踪 | 白名单档案跟踪，*.log 排除 |
| `tools/archive/desktop_20260826` | 65.8 KB | 跟踪 | 小脚本归档，非大文件 |
| `tmp/desktop_leftovers_20260831` | 41 KB | 跟踪 | 小诊断脚本，非大文件 |
| `assets/tianji.ico` | 小 | 跟踪 | 图标资源 |
| `web/vendor/echarts.min.js` | 0.98 MB | 跟踪 | 1MB 级文本可接受 |

## 2. dry-run 眼扫记录（`git add -An` → tmp/w1_dryrun.txt）
- **入库文件总数：667**（`git add -An` dry-run 行数），解析 666 个实际路径。
- **漏网大文件（>5MB）：0 个**（脚本按实际文件 size 逐文件核验）。
- **可疑二进制扩展名（.db/.sqlite/.bak/.lock/.png/.jpg/.zip/.exe/.dll/.bin/.whl/.pyc/.pkl/.joblib）：0 个**。
- 顶层分布：data 235、tmp 140、tools 121、docs 103、app 47、root 8、legacy 6、web 6、assets 1。
- 眼扫结论：data/ 全为小文本（bt_*.json、attribution/、audit.jsonl）；root 8 个为 .gitignore/README/main.py/探测脚本；**未发现 market.db、min5.db、snapshots、backups、*.bak 漏网**。

## 3. 基线提交结果（实测）
| 项 | 值 |
|---|---|
| 提交 hash | `9aeb55652d16ec3d6505c1bbe421526eb5a31221`（短 `9aeb556`） |
| 提交信息 | `baseline 2026-09-01: post-incident full tree (code+docs+small data)` |
| tag | `baseline-20260901` |
| 入库文件数 | **668**（含本次 .gitignore 自身与 dry-run 审计痕迹 w1_dryrun.txt / w1_audit.py） |
| `git count-objects -v` size-pack | **4.68 MiB**（红线 <80MB，达标，约 1/17） |
| in-pack 对象 | 700 |
| 工作区行尾 | app/trader.py 仍为 LF（1585 bareLF / 0 CRLF），**未被 git 改写** |
| 仓库级配置 | `core.autocrlf=false`（防未来 checkout 把 LF 转 CRLF 破坏项目 LF 约定） |

## 4. .gitignore 清单与理由（27 行，5 分组，见项目根 `.gitignore`）
1. **大二进制/GB 级数据**：`data/market.db*`（覆盖 1.2GB 主库 + 2 个 .bak）、`data/min5.db*`、`data/snapshots/`、`data/backups/`、`data/webcache/`。
2. **虚拟环境/编译产物/缓存**：`.venv/`、`tools/ml_sidecar/.venv/`（显式冗余，防 `**` 匹配歧义）、`**/__pycache__/`、`*.pyc`。
3. **日志与运行输出**：`*.log`、`*.err`；**白名单恢复**：`!tmp/c_lane.log`、`!tmp/coord_*.log`、`!tmp/watchdog.log`、`!tmp/pack*/`（提示词档案有审计价值）。
4. **运行时状态锁**：`data/app.lock`、`data/audit/.write.lock`（0KB 瞬时锁，不入库）。
5. **桌面无关物/临时残留**：`Desktop`。

## 5. 夜间任务批准矩阵 + 一键可批命令
> 走批准门：用户 **2026-09-01 批准**，命令执行后**注册成功**，无需降级通道。

**批准矩阵**：
| 项 | 值 |
|---|---|
| 用户批准 | ✅ 2026-09-01（本轮回复"批准"） |
| 注册命令 | `schtasks /create /tn "TianjiGit_Nightly" /sc daily /st 23:50 /tr "powershell -NoProfile -ExecutionPolicy Bypass -File '...\tools\git_nightly.ps1'" /f` |
| 注册结果 | SUCCESS（exit=0） |
| Next Run | 2026/9/1 23:50:00 |
| Status | Ready |
| Task To Run | `powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\26838\A股模拟盘\tools\git_nightly.ps1"` |
| Schedule Type | Daily，Start Time 23:50:00（与 16:30/20:00/20:30 任务窗无冲突） |

> 备注：`schtasks /sc daily /st` 对当前用户**实测可用**（无需管理员）；H1 曾遇阻的是 onstart/onlogon 触发器，不适用于本任务。

## 6. operations.md 追加节 diff 摘录
> 在 `docs/operations.md` 末尾追加 **「版本控制 SOP（2026-09-01 起）」** 节（UTF-8/LF，追加后 8707 字符，CRLF=0/bareLF=203）。摘录如下（完整见文件末尾）：

```markdown
## 版本控制 SOP（2026-09-01 起）
### ① 基线 / 回滚命令
- git diff HEAD~1 -- <path>  /  git diff baseline-20260901 -- <path>
- git checkout baseline-20260901 -- <path>（恢复单个文件）
- git checkout baseline-20260901 -- .（完整回滚，谨慎）
### ② 执行 AI 改共享文件纪律
任何执行型 AI 改动共享文件前后，必须把 git status --porcelain 完整贴进交付报告；
提交由 nightly 自动任务统一落库，执行 AI 不手动 commit。
### ③ 数据大文件边界声明
data/market.db*、data/min5.db*、data/snapshots/、data/backups/、.venv/ 不在 git 版本内，
保护仍以文件层备份为准；"git 能找回大文件"是错误假设。
### ④ 夜间自动提交
TianjiGit_Nightly（23:50）→ tools/git_nightly.ps1：git add -A → auto commit（无变更吞非零退出）→ tmp/git_nightly.log
```

## 7. 本块未做的事（留给验收/后续）
- ~~计划任务 `TianjiGit_Nightly` 尚未注册~~ → **已获批准并注册成功**（见 §5），今晚 23:50 首次运行。
- 未对 W2-W5 并行产物做任何处理（`tmp/w2/probe2.py`、`data/ak_probe_results.json`、`tools/ak_probe.py` 等已存在且未跟踪，将自然混入首次夜间 auto commit，属预期）。
- 未推送任何 remote、未 fetch（红线）。
- 未改动 app/、tools/ 既有代码与既有报告正文。
- 基线后未做任何二次 commit（当前 `git status --porcelain` 的变更将全部落入今晚首次 auto commit）。

## 8. 验证方式与缺口
- 验证：`git add -An` dry-run 眼扫（§2）、`git count-objects -v`（§3 4.68MiB）、`git log/tag` 确认、AST 解析 ps1 语法 OK、operations.md 编码/LF 实测、计划任务注册核验（§5：Next Run 23:50 / Ready）。
- 缺口：`tools/git_nightly.ps1` 未实际运行测试（避免违反"只 commit 一次"红线）；其"无变更吞非零退出"逻辑经 AST 语法验证，**今晚 23:50 首次计划任务运行自然验证**（届时应产生一个 `auto 2026-09-01 23:50` 提交并写入 `tmp/git_nightly.log`）。
