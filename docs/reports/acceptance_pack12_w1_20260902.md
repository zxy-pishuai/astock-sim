# 验收 pack12/W1｜首板回踩战法历史回测（验收方 2026-09-02 23:05 独立取证）

**总分 63/100 —— 不通过（有条件返工）**。判据机制层全对、字节级可复现；
但**样本分段执行层存在致命缺陷：预注册的 IS/OOS 从未真正分开**，导致报告
核心叙事（"OOS 无衰减反而更强"）无效，且真 IS 上所有被测组合期望转正失败。

## §1 通过项（独立重算，字节级一致）

我用 `tmp/w1v.py`（py -3.13，直接读快照，不复用其缓存）按预注册 §0 重建：
- 漏斗 28,712 → 19,789 → 12,821 → 9,533：**逐级精确一致**；
- 热力基线 k3/K10 出二板 0.232：**精确一致**；
- 涨停比值判据+除权 ±1 日隔离、成本公式（0.225%/0.275%）、E1/E2/E3 触发逻辑、
  一字/涨停收盘剔除——读码核对与 §0 相符；
- 时间线干净：预注册 22:00:52 落盘先于全部跑数（grid 22:14、yang 22:33、
  报告 22:35），§0b 另开新节而非改 §0，P72 程序意识在线；
- 写白名单：git status 交叉核验，只写了 5 个允许位；Z3 资产哈希基线未动
  （028A67F9/34271166 复验通过）。

## §2 致命缺陷：IS 窗口泄漏（实锤链）

1. **代码**：`tools/tactic_backtest.py` `event_mask()` 只有 `start` 无 `end`；
   `cmd_grid()` 以 `IS="2019-01-01"` 只作下界（L297-303、338），
   `detail_nofence.py`、`re_run_yang.py` 原样继承（各 L10-11）。
2. **自证**：交付 JSON `sens.base.by_year` 含 **2024/2025/2026** 三年胜率
   （0.1398/0.157/0.1914）——"IS"格子里坐着 OOS 年份。
3. **量化**：FULL(他们当IS)=n 9,170 / OOS=3,640，相加≈全窗 9,533；
   **真 IS(2019-2023)只有 n≈5,526**。
4. **后果（我的三窗口重跑，同判据同路径函数）**：

| 组合 | 报告宣称(IS) | 真 IS | 报告 OOS |
|---|---|---|---|
| base E1_k7 | +0.5% | **−1.02%** | +2.81% |
| 主推荐 E3_5pct_next+V2_H20 | +2.55% | **−0.62%** | +6.53%（此值本身算对了） |
| 澄清2主推 E2_ma10_yang+yang9_next_H14 | +3.51% | **−0.82%** | +12.62% |

   即：**2019-2023 真样本段上没有一个被测组合期望为正**；正收益全部来自
   2024-2026。且因为选参网格在全窗（含 OOS）上跑，OOS 早已被选择污染，
   "一次性终验"的纪律实际未成立。报告 §3"无衰减、反而更强"叙事作废。
5. **定性结论幸存**：k3 越晚越差的梯度、E3 确认提升 winB、三关不提 winB、
   阈值越高 exp 越好的排序——真 IS 上方向不变（我重排过 yang 网格，最优
   仍是 yang9_next，只是数值转负）。坏消息是结构可信、数值全需重刷。

## §3 次级问题（返工一并处理）

- **报告↔JSON 不一致**：§4 规则卡写 `yang_pct: 7`，交付 JSON `recommended`
  实为 `9`——W2 将逐字消费 JSON，报告口径必须与其同步（或显式双版本理由）；
- **finalize.py 手抄数字**：events_funnel 八个数是硬编码进交付 JSON 的
  （L30-34），本次经我复算恰好为真，但"手抄=迟早抄错"，要求程序化；
- **`_meta` 2427 vs 正文 2354**：前者含指数/ETF 前缀未过滤，同文件自相矛盾；
- **预注册页眉"22:3x" vs 实际落盘 22:00:52**：时间戳写错（内容以 mtime 为准，
  不构成事后修改，但要澄清）；
- **§0b"用户澄清 2"溯源——已结案（23:20）**：人类向验收方证实系其本人直接
  对执行 AI 所言（"意思差不多…都是等回调之后的大阳线"），**§0b 合法，无红线
  事件**。人类同时给出新口径"大阳没封涨停就当日卖，封了涨停格局到第二天吃
  溢价"——已以 **§0c 混合卖点 M 模式**预注册进 W1R（新增被测组合，不替换
  原网格）。

## §4 处置

- W1 **不得进入 W2 派发前置**；`recommended_rule`/`yang_rule` 的 exp 类数字
  一律视为"全窗口混合值"禁用；
- 修复块 = **pack14/W1R**（`tmp/pack14/W1R_true_is_regrid.md`）：真分段重跑
  + OOS 污染显式申报 + 交付 JSON 程序化重建 + 报告规则卡按真 IS 重写
  （不回避：若真 IS 全负，结论就是"该战法 2019-2023 不成立、2024 后吃 β"）；
- 评分构成：判据翻译与实现忠实 +30（机制层满分），P72 程序 +15，漏斗/热力
  可复现 +18（合计 63）；窗口纪律崩坏 −25，OOS 污染 + 规则卡失真 −12，
  手抄/不一致/溯源 −4（部分在 §3）。
