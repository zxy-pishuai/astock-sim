# 五任务批次验收报告（2026-08-27 批：任务1–任务5）

- 验收时间: 2026-08-30 01:45–02:05 ｜ 验收方: ox-alpha（同会话自验收，见 §6 独立性声明）
- 验收依据: `tmp/acceptance_prompt_tasks_1_5.md`（验收提示词，含纪律与必查项）
- 纪律执行: 全程只读（唯一项目内写入 = 本报告）；**未重启服务**；
  market.db 与快照零写入；唯一例外见 §5 第 3 条（验收工具内建行为）。
- **总判定: 整批有保留签收 —— 任务1/2/3/5 ✅ 通过，任务4 ⚠️ 有保留通过**
  （保留项已量化、不翻转结论；附 2 项批外发现 + 1 项整改建议）

## 0. 验收前后状态对照

| 项 | 验收前 (01:45) | 验收后 (02:05) |
|---|---|---|
| 8899 端口 | **未监听** | 未监听（一致，无重启） |
| 项目文件 | — | 仅新增本报告；`data/audit_chain_breaks.json` 被诊断工具按设计刷新（见 §5.3） |

> 注: 8/27 批执行时服务 PID 21728 在跑（各报告均有前后核验）；验收时服务已停
> （停机时点不明，属批外状态变化）。三个计划任务为独立进程冷启动，不受影响。

## 1. 任务1｜审计链统一 —— ✅ 通过

证据:
1. `app/trader.py` L380 `audit.record("order","strategy_buy",...)`、L413
   `strategy_sell` 在位；`app/audit.py` L39-46/124-150 `.write.lock` 字节锁 +
   `os.fsync` 落盘同步在位（grep 实证）。
2. `python tools\audit_chain_diag.py --verify data\audit\audit.jsonl`:
   断裂合计 **340** = 339 基线 + 行 **2124**（2026-08-27 10:58:18，任务1 报告 §4
   已逐字记录的旧版服务单条自愈断链），**其后零新增** —— 新写入路径健康度与
   报告声明一致；`strategy_*` 子序列无断裂。
3. 存档 `data/audit_chain_breaks.json` 结构完整（行号/时间/性质/prev 对照）；
   历史 339 处只标记未修补（红线遵守）。
4. 语法编译: 全项目 `app+tools(+ml_sidecar)` **135 个 .py 编译 0 失败**
   （报告称 155 个，口径差 = 本验收未计 legacy/web/tmp，无实质出入）。

备注（不扣分）: "account.json 结构未动"无修改前副本可比，记未核验（低风险，
钩子为成交记账后的纯追加调用，代码路径审读无写入）。存量 pos_cash
先用后赋缺陷按红线只记录未修（`docs/backlog.md` #9 已登记）。

## 2. 任务2｜sector_map 治理 —— ✅ 通过

证据:
1. `data/sector_map.json`: **2,911 键 / 49 行业 / 0 坏格式**（键全 6 位纯数字、
   值为 `[行业名]`），与报告 §0 修复后行逐项一致。
2. `data/sector_map_refresh_probe.json`: `started_at 2026-08-27 11:34:07`
   （午休非交易时段，等待 27.3s 后执行）、verdict **"OK grown"**、唯一残差
   `new_stock got 84/151` 与报告 §4 归因（北交所标的被 sh/sz 过滤，非缺陷）一致。
3. `app/sector.py` L158 `_shrink_guard`、动态分页与遥测 `_last_fetch_stats` 在位。
4. `forward_eval.py` mtime 07:30 早于任务2 执行窗（10:16+）→ 零改动属实。

## 3. 任务3｜C 通道重派 —— ✅ 通过

证据:
1. 快照口径属实: `tools/c_lane_redispatch.py` L39/76-80/120/176 —— worker 内
   `C.DB_FILE=固定快照`、只读 URI、`meta.snapshot` 与报告一致。
2. `data/bt_c_lane_redispatch.json` **11 变体 × 4 窗 = 44 格 runs**；抽查逐位一致:
   act8_T4 = {+1.08/−40.57/−22.92/+54.59}%、C5_T2A1 = {−12.26/+26.88/+13.60/+54.12}%
   与报告表格完全一致；`judgement` 9 变体的 windows_ok/bull_loss_pp/pass 与
   报告判定表一致（判定规则 ≥3/4 窗 Δ≥−1pp 且牛窗 ≤2pp 复算吻合）。
3. **C3"两档逐位相同"直接验证**: `C3_trig10_back5` 与 `C3_trig12_back6` 四窗
   收益/交易数完全相同（10.28/1.78/1.70/−1.71）——高档退化为同一行为的结论成立。
4. C4 数据门控: `c4_data_gate.available=false` 附量化事实（north 299 行单日截面、
   rzrq 覆盖率），**blocked 是纪律的正确执行**而非缺陷。
5. 同日重跑 base（`base_score`/`base_board`）→ 内部 A/B 单变量自洽。

## 4. 任务4｜大盘状态门控 —— ⚠️ 有保留通过（结论稳健，报告需小改）

通过项证据:
1. **预注册先于评估**: `tools/regime_gating.py` mtime 11:30:48 < 主跑
   `generated_at` 11:31:12；判据原文与报告 §0 一致。
2. **三段复算全过**: ① G2 判定由 `gated_backtests` 复算与 `gating_verdicts`
   四臂全一致；② 16 个 parts 与主 JSON 逐格一致；③ 钉主跑快照复算标签，
   **年度计数与 `state_shares_by_year` 逐格一致**（同时复核了 §6 快照漂移
   事故处理声明：v1 作废、v2 钉快照）。
3. G3 数字与判据吻合（V1 允许 74.4%/最差窗 81.4%；V2 26.7%/最差窗 18.0%）。

瑕疵（保留理由，已量化，**不翻转结论**）:
- **宇宙不一致**: 基线 parts 生成于 8/26 21:25–8/27 02:00（排除清单 197，含
  002594）；门控臂运行于 8/27 10:47+（**196** —— 002594 已于当日 07:30 被批外
  移出，见 §5.1），而 002594 ∈ pool[:500]（index 210），且**快照内 002594 为
  旧污染序列**（`byd_repair_002594.md` 建议②明确警告过该风险）。任务4 钉了
  数据库却没钉宇宙。
- **去混淆量化**（用 C 批同日 196 口径 base 反推，`byd_effect = C_base − parts`）:
  score [−3.23, +0.70, **+13.97**, +8.12]pp、board [0, +5.08, +1.11, 0]pp（W1-W4）。
  去混淆后 G2 复评: V1/board fail、V1/score fail、V2/board fail、
  V2/score pass→仍被 G3（26.7%<35%）拦截 —— **四个判定全部不变，"全部不落地"
  结论稳健**；但报告单窗 Δ 数值最多带 ~14pp（score W3）的宇宙偏差。
- 报告文字: §1"非 DATA_EXCLUDE_CODES(197)"与实际运行口径 196 不符。
- 暴露面补充: 等权线在 2025-07-17 单日含 002594 假接缝（对线约 +0.46% 单日
  扰动，落在全部测试窗之外，仅影响 W4 起始数日的 r20 回看）；IC 节 W4 日
  含 002594 旧序列秩噪声。均为备注级。

整改建议（验收后由执行方落实，本次不动文件）:
① regime_gating.md §1 更正为 196 并注明批外时点；② 附录补去混淆表
（本文 §4 数据可直接引用）；③ `bt_regime_gating.json` 增补
`universe_confound` 块。

## 5. 批外发现（如实记录，不计入五任务判定）

1. **8/27 07:30:18 `app/config.py` 与 `tools/forward_eval.py` 同秒变更，无报告、
   无审计事件**（audit.jsonl 查 002594/07:3x 零命中）。config 内容反推 =
   002594 移出 `DATA_EXCLUDE_CODES`（197→196，与 byd 报告建议①一致，变更本身
   合理）。**变更无留痕违反项目变更纪律** —— 建议补一条 backlog 登记。
   `docs/backlog.md` 至今仍写"建议移出清单待验收"，与现状不符。
2. **服务已停**: 8899 未监听（8/27 时 PID 21728）。计划任务独立冷启动不受影响；
   但若盘中服务需要 sector_map 新口径/审计新代码，需人工重启（非验收动作）。
3. **验收方工具行为披露**: `audit_chain_diag.py --verify` 按设计刷新
   `data/audit_chain_breaks.json`（mtime 更新、339→340，补入的正是报告 §4
   已记录的行 2124，内容为超集无信息损失）。

## 6. 独立性声明

任务4 由本会话执行、本会话验收，独立性有折扣；任务1/2/3/5 为其他批次产物，
本验收为其首个完整复核。如需完全独立复验：`tmp/acceptance_prompt_tasks_1_5.md`
含全部必查项与复算脚本，可交 fresh-agent 以 ~10 分钟复跑本文 §2-§4 关键证据。

## 7. 验收运行清单（全部只读）

`schtasks /query ×6`；`audit_chain_diag --verify`；全项目 py 语法编译（135 个）；
sector_map/probe/c_lane/regime 四个 JSON 的程序化复算（含钉快照标签复现与
去混淆 G2 复评）；grep 级代码在位性检查（trader/audit/sector/config）；
LF 抽查（7 个关键交付物 0 CRLF）；3 个 .cmd ASCII 检查（0 非 ASCII 字节）；
8899 端口前后对照。复算脚本存于 `%TEMP%\acc_recheck.py`（项目外）。

## 8. 签收

| 任务 | 判定 | 一句话依据 |
|---|---|---|
| 任务1 审计链统一 | ✅ | 钩子/单写者在位；链健康 340=339+1(已声明)；零新增断裂 |
| 任务2 sector_map 治理 | ✅ | 2911/49/0 坏格式；probe OK grown；缩水保护在位 |
| 任务3 C 通道重派 | ✅ | 44 格逐位一致；判定复算吻合；C4 blocked 系纪律执行 |
| 任务4 大盘状态门控 | ⚠️ | 三段复算全过、结论经去混淆后稳健；宇宙不一致已量化、报告需小改 |
| 任务5 运维补全 | ✅ | 6 任务 Ready、日志按节奏、cmd 纯 ASCII、未动既有任务 |

**整批有保留签收**。签收条件: 任务4 三项整改建议落实后，⚠️ 可升级 ✅。

## 9. 整改落实记录（2026-08-30）

任务4 三项整改已由执行方落实并经本验收方复核:

1. ✅ `regime_gating.md` §1 更正为运行时实际口径 196 并注明批外移出时点；
   §4 判定表后增宇宙偏差提示。
2. ✅ `regime_gating.md` 附录 A 落盘: 事件链（07:30 批外移出、无留痕）、
   去混淆方法、byd_effect（score [−3.23,+0.70,+13.97,+8.12] / board
   [0,+5.08,+1.11,0]）、去混淆 Δ 表与 G2 复评（四判定不变）、暴露面备注。
3. ✅ `bt_regime_gating.json` 增补 `universe_confound` 块（byd_effect_pp /
   deconfounded_delta_pp / g2_reeval），数值与本文 §4 逐位一致，JSON 可解析、
   LF 保持。

另按 §5.1 建议补记: `docs/backlog.md` P71 行"建议移出清单待验收"更正为
"已于 08-27 07:30 移出（197→196）"并新增第 10 条验收记录。

复核方式: JSON 程序化断言（键存在 + 关键数值逐位）；md 人工比对引用数值。
**任务4 ⚠️ → ✅，整批签收升级为无保留通过（5/5 ✅）。**
