# 验收｜C4 ATR 退出对照 + R3 缺席说明（验收方 2026-09-10 深夜）

**C4：通过 96/100（null 归档）；R3 未执行（执行 AI 自报"R3 未做过"）。**

## C4 核验

- **§0a 调整的定性**：执行 AI 在跑批前补了 5 条执行口径（激活前 −7% 固定保护、
  ATR 静态确认、对照纯度声明、IS/OOS 边界确认、20 日强卖兜底）——**判过判据
  本身一字未改**，属预注册前的消歧而非 P72 违规，**追认**。其中 −7% 前置保护
  与 20 日上限是必要消歧（否则 T 臂变无限持有稻草人）。
- **基线逐位复刻 V1**（IS +2.56%/n2709、OOS +2.28%/n3002，±0.01pp）✓；
- **n 守恒**：5711=valid+剔除 三臂全对 ✓；**判定表机械复核**：T1 死于 cond1
  （IS +0.02 vs 门槛 +2.46）、T2/T3 死于 boost 不足（+0.39pp/+0.28pp < 0.5）✓；
- **自检**：2 臂×20 笔独立复算 0 差异 + 双 ATR 实现一致 + 3 笔逐日人工复核（自报，
  抽查项）；
- **结论采信**：ATR 跟踪（含延长持有）对 A1 打板体系**无增益**——打板的 edge 是
  隔夜高开溢价，延长持有把它耗成 0（IS 2022/2023 转负）。**现行"次日开盘卖"
  维持不动**。§0a.3 纯度声明正确：这不是"ATR 更差"的强结论，是"延长持有在
  打板体系不成立"的证据。
- **对内核 K2 的修正**：C4 是反方向证据——K2 的"波动率归一化"方向对打板体系
  **不成立**；K2 降级为观察项，未来退出优化的入口在别处（报告 §8：入场端）。
- 红线：零 app/ 改动（git 仅见已验收轮的 audit.py 与运行时状态文件）✓；
  白名单合规 ✓。
- 幸存者偏差与排队成交近似已披露（§7）——与 V1 同款，不新增扣分。

## R3 复核修正（原"缺席"结论撤回）

误判根源：①查重门 12:02 的"R3 未做过"是**开工前检查**，被误读为终态；②22:44
扫描窗口（近 10h）恰好切掉 12:0x 落盘的 R3 文件。**R3 实际于 09-10 中午完成，
判定：通过 94/100。**

- **E1 已由验收方活体验证**：`idx_kline_pcd` 存在于 sqlite_master（连同既有
  cp/pd 索引不重复）；报告：备份 1.3GB SHA256 match、非交易时段引擎未运行时
  执行、建索引 11.4s、聚合查询 **58.2s→0.83s（70.3 倍）**、EXPLAIN 走
  COVERING INDEX 无 TEMP B-TREE、回滚一句 DROP。验证指标从单票排序改为聚合
  计时属**合理评审调整**（单票排序本就走主键索引，原指标量不出增益）。
- **E2 追认其白名单扩展**：报告揭示了任务书没想到的**竞态**——外部 rename 与
  `record` 的 `_cross_proc_lock` 不互斥 → 落为 `app/audit.py +45 行 rotate_daily()`
  持锁入口（超出我白名单的 app/audit.py 改动，功能上正确，追认）；保留 90 天、
  双命名兼容、归档后确保活动文件存在；试跑行数守恒 4,471 行、追加测试首条
  prev="" 链语义正确、幂等重跑通过；计划任务 TianjiAuditRotate 已注册
  （00:05/日，回滚 schtasks /delete 一并留档）。
- 两个追认调整的共同点：**先评审后执行、逐条留痕、试跑验证后才注册**——这是
  执行纪律的正面样本。
