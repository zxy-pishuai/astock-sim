# 验收报告｜H1 交付核验 + K1 复核（2026-09-05 周六 22:1x-22:3x）

**总判定：H1 🔴 不通过（0 分，性质是"未交付"而非质量差）；K1 ✅ 通过 94 分（停手决定经我方独立勘查成立）。附带认领一项验收方自身的派发缺陷：块代号 H1 与 8/31 老报告撞名，已直接导致一次误判。**

| 块 | 判定 | 分 | 依据 |
|---|---|---|---|
| pack19 H1（看门狗恢复死锁根治） | 🔴 未交付 | 0 | 生产文件零改动、无交付报告、无查重门记录、无备份留档（四项全缺） |
| pack20 K1（行情库真实副本） | ✅ 通过 | 94 | "本机无第二介质故停手"经我方独立勘查逐字复现；零副作用（未备份/未注册/未删除/未改码） |
| 验收方自检 | ⚠️ 缺陷 | — | 块代号 H1 复用旧名，未做全局唯一性检查 → 已在 H1b 修订中堵死 |

## §1 H1 未交付：五条独立证据

1. **代码零改动**：`rg -n "capture_output|DEVNULL|close_fds|os\._exit|TIMEOUT-BAIL|finally"` 于 `tools/service_watchdog.py` → 仍为 `start_service()` 内 L379 与 L391 两处 `capture_output=True, timeout=30`，无 DEVNULL、无 close_fds、无进程级超时自退、无 try/finally 状态兜底。
2. **git 证明**：`git log --oneline -- tools/service_watchdog.py` 最后一次变更是 `77a02cc`（9/4 23:50，即 R2 那次）；`git status --porcelain` 中该文件不为 M → 工作区与 HEAD 一致，今天下午至晚上无人改过它。
3. **交付报告不存在**：`docs/reports/` 内无 `h1_watchdog_deadlock.md`（本日下午至 22:16 唯一新增报告是 `k1_backup_policy.md`）。
4. **查重门未落**：`tmp/pack19_dup.log` 文件不存在（H1 第 0 步为强制项）。
5. **改前备份缺**：`tmp/pack19/` 下只有 13:00 生成的 5 份派发文档，无 `backup/`、无任何取证产物。

## §2 死锁今日第三次复现（现场完整）

- 21:55:46 整机第二次开机（今日第二次）→ 22:00:01 计划任务拉起 `service_watchdog.py`（PID 22844）→ 22:00:06 记"调度断更 35100s"+"异常 **5**/3"+"恢复触发" → 22:00:08 记"端口 8899 关闭（全死），直接重启" → **此后 24 分钟零日志、零心跳、零状态写入**，进程 22844 仍存活。
- 因 `MultipleInstancesPolicy=IgnoreNew`，22:00 之后每一轮激活继续被吞 → **服务 24636 虽然健康在跑，但又是无人守护状态**。
- 计数只增不清：`h1_watchdog_state.json` = `consecutive_failures: 5`、`restart_ts: ""`。序列 3（9/3 11:35 卡死留痕）→ 4（12:15）→ 5（22:00）与"每次卡死后永不清零"完全吻合，也再次印证 §1：修复未发生。
- 今日时间线小结：12:11 开机 → 12:15 复活 8899（守护随即卡死）；21:55 开机 → 22:00 复活 8899（守护随即卡死）。**两次都是靠"人登录 + 5 分钟任务"侥幸救回，恢复动作本身每次都成功，代价是看门狗每次都自我作废。**

## §3 误判根因：块代号撞车（验收方认领）

- `docs/reports/watchdog_hardening.md`（**2026-09-01 00:03 写**，正文首行"# **H1**｜看门狗硬化 + 半死状态根治"）是 8/31 那一轮的 H1 交付。
- K1 执行方在 `docs/reports/k1_backup_policy.md` §0 写道："派发时机核对：`docs/reports/watchdog_hardening.md` 存在（pack19 H1 已交卷）" → **凭文件名与标题前缀误推 pack19 H1 已完成**。
- 责任在派发方：我复用了已被占用的代号 H1，且未在全项目做唯一性检查。已修订：本块改号 **H1b**，并在查重门中要求 grep `docs/reports/` 内以同一代号开头的历史标题，命中即须列清单并声明区别。

## §4 K1 独立复核：通过 94

- 我方独立勘查：`Get-Disk` 仅 Disk 0（NVMe `KPEGYMTBW2U001TCJD`，954GB，GPT）；`Get-Volume` 仅 `C:` 为可写数据卷（952.1GB / 剩 186.4GB），另两个为 Recovery 与 EFI 无盘符分区 → **确无第二介质**，K1 §1 结论逐字成立。
- 备份任务侧复核：全机任务表内 `TianjiDataBackup` 零命中 → 与"未注册"陈述一致（其停手时不注册是正确的）。
- 加分：§6 清理清单只登记不删除（`data/market.db.bak` 63.0MB / `data/market.db.bak.20260819_011241` 63.4MB / 旧固定名 `watchdog_child_stderr.log` 209KB），符合红线；§7 明确请求人类决策而非自行退化同盘拷贝。
- 扣 6 分：其 §0 的"派发时机核对"援引错误依据（§3），虽不影响其停手决定的正确性，但属判据来源不严谨。
- 顺带澄清一件可能被误读的事：`C:` 剩余空间从今日 13:02 的 201.2GB 降至 186.4GB，**但本项目占用无变化**（`data` 14.06GB、`market.db` 1458.8MB、`min5.db` 724.6MB 与午间逐字节一致，`tmp` 1.74GB）→ 那 15GB 不是本项目写的，无需在盘内追查。

## §5 排序修正：H2 现在就该做，且优先于 H1

理由（今日现场给的）：`ExecutionTimeLimit=PT2M` **不需要改一行代码**，就能把"永久自锁"降级为"每次最多 2 分钟守护缺口"——卡死实例会被任务计划器强杀，下一轮 5 分钟激活不再被 IgnoreNew 吞。也就是说：

- 只做 H1 不做 H2：仍有代码路径之外的挂死风险（未知阻塞 → 依旧自废）。
- 只做 H2 不做 H1：守护链每 5 分钟恢复一次新鲜进程，服务不再有"整段裸奔窗口"，但 `restart_ts`/STARTUP_GRACE 仍不落地。
- **H2 是今天两次开机裸奔的唯一低成本解药，建议今明两晚就由你在场执行。**

## §6 待你决策

1. **H2 现在批？**（改 4 个任务设置字段，你在场点确认即可，约 5 分钟；含"电池可启动 + 漏跑补跑 + 2 分钟超时强杀 + 保持 Interactive 身份不变"）
2. **H1b 派给谁**：纯代码块，无外部依赖，可与 H2 并行。
3. **K1 后续**：(a) 你接一块外接盘/NAS 映射，我让 K1 续跑真实副本 + 恢复演练；(b) 接受单盘现状，我把"行情库零副本"登记为长期风险（不催）。
4. 眼下挂死的 22844：H2 落地后会被自动强杀；或你现在手工结束它（**验收方按红线不动进程**）。

## §7 诚实披露

- 本报告全程只读，写入仅本报告 + `tmp/pack19/` 两份修订文件；`git status` 可证未碰生产文件（当前 M 项均为运行时数据与看门狗自身产物）。
- §1 判定"未交付"针对**交付物与代码事实**，不评价执行方是否曾尝试；若他们另有产出放在别处，给出路径我立刻补验。
- §2 的"24 分钟零输出"是采样时刻读数，未做连续跟踪；但结合 `IgnoreNew` + 进程存活 + 状态冻结，判定为挂死而非"未触发"。
- 服务本身此刻健康：22:24:06 `GET /api/overview` HTTP 200，`time` 字段与请求同秒（引擎在跑），regime=强势 / max_pos=3 / threshold=25 / position_pct=0.3 / market_count=5014；账户 `cash`=47455.098、26 笔成交未变。