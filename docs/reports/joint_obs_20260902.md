# 9/2 收盘联合观察报告（Z5）

- 日期：2026-09-02（周三）20:50-21:00 执行
- 执行者：Z5（纯只读取证）
- 观察窗：18:30 后 ✓（实际 20:50 开始）
- 查重门：`docs/reports/joint_obs_20260902.md` 本次创建，无 DUP
- 说明：X5 9/1 版判据清单并入本报告（9/1 分支已闭合项引用既有验收报告，不重复采样）

---

## 0. 一句话总评

> **9/2 = 首两交易日链闭合（09:30 卖出 4 笔 + 11:18 买入 + 收盘更新 + 双日快照 + 20:00/20:30 双任务全跑通），但有运行时缺口：8528 午后（13:00 复市后）引擎主循环未恢复、心跳停写、overview 卡死，19:50 起被 watchdog 反复半死重启（8528→42508→43948→47096→44760）；experiments 索引不自动刷（9/1 后未更新）；regime 无历史快照。系统当前可信度评级：B-（数据面与任务链可信，运行时半僵死隐患未除，需 Y2 查 overview 卡死根因）。**

---

## 1. 取证清单逐项结果

### ① 16:30 台账 —— ✅ 静默正常（非故障）

| 项 | 实测 |
|---|---|
| ml_scores_ledger.jsonl | 57 行，mtime **9/1 16:30:31 未变**（末行 realized_at=9/1 16:30:31） |
| 任务 ml_scores_ledger_daily | **LastRun 9/2 16:30:00，Result 0（成功）**，Next 9/3 16:30 |
| 任务日志尾部 | "信号日 2026-08-26 总分=1700 追加 0 行"（9/2 tick 0 行） |

**分类：静默正常（无新信号）**。ml_scores.json 生产端仍 8/26（D 块批准未批），8/26 信号日 ret 已全部实现，9/2 tick 追加 0 行属预期。**非任务故障，P1 不上报**。

### ② 20:00 risk_daily / 20:30 forward_eval —— ✅ 双任务全跑通

| 项 | 实测 |
|---|---|
| forward_eval.jsonl | **已新增 9/2 锚日行**（recorded_at 20:30:04，hint/news 均空，realized=null 待 9/3 结算）；**9/1 行已结算**（realized date=9/2，bench_pct −0.60%） |
| forward_eval 任务 | LastRun 20:30:01，Result 0 |
| risk_daily | **risk_daily_2026-09-02.md 已生成**（20:00:01，3408B）；任务 Result 0 |
| risk_state.json | mtime 9/2 9:30:43（盘中熔断态：consec_losses=0/cooldown=""/breaker=false）——由 9:30 写入，与 20:00 的 .md 报告分离，正常 |

### ③ 600121 首笔"新引擎周期买入"质量复核 —— ⚠️ 有疑点，仅记录

- **11:18:21 8528 买入 600121 郑州煤电 2600 股@5.425**（audit strategy_buy 实锤），评分 93 分，策略标签 **"[弱势(抱团)]"**：量价齐升(+20)、放量突破20日新高(+20)、均线多头(+15)
- 收盘 5.30 → 浮亏 **−325 元（−2.30%）**，市值 13780
- **疑点 1（时间设计意图）**：11:18 买入**非 09:30 竞价后 8 分钟**，也非现役"两点半选股提醒"窗口（15:00-15:30 的 `_twothirty_round`）——由主循环 `_maybe_launch_scan_async` 异步扫描触发，属正常主循环路径，但任务书标注"新引擎周期买入"的时间特征需记录
- **疑点 2（regime 合规性无法直接证实）**：当前 overview regime=**崩溃(防守)**、max_pos=0、threshold=99；9/2 上证 −0.97%/深成 −1.88%/创业板 −2.39%（大跌日）。但 **data/ 下无 regime 历史快照**（仅 risk_state 熔断态），audit 无 regime/risk 事件 → **11:18 买入时点 regime 无法回溯**。按"行为异常只记录不修"原则记录：若 11:18 已近崩溃防守阈值而买入，属边界操作；需下一批补 regime 日志后再判定
- 结论：买入执行链完整（audit+account 一致），质量疑点 2 项记录待证

### ④ 心跳形态 —— ⚠️ 实锤异常：8528 午后主循环未恢复

**心跳语义（读 trader.py L307-370 确认）**：`while self.running` 主循环，交易时段走完整扫描分支并 `if int(time.time())%60==0: al.heartbeat("trading")`（L367-369）；**非交易时段走 L338 `time.sleep(30); continue` 分支，不写心跳**。即 heartbeat = "盘中主循环存活"标志。

**9/2 实测分布**：
```
09:00-11:00  heartbeat=33, trading_event=154   ← 8528 上午正常
11:22:00     最后一条 heartbeat（8528 午休前）
11:23-15:33  audit 仅 2 条（15:31 trading_event）—— 12/13/14 点全空
15:34:07      data_update（updater 独立线程写）
15:34:09      data_update + data_update_failed（Z4 双跑诊断中）
15:34:15      data_snapshot（9/2 快照）
19:40-20:35   8528 链 overview 持续 TimeoutError → watchdog 反复半死重启
```
**结论：8528 午后 13:00-15:00 交易时段无 heartbeat = 主循环未走交易分支（`_is_trading_time` 判定后未恢复或主循环卡死）→ 午后无交易扫描 → server overview 卡死 → 19:50 被 watchdog 判死**。这与 9/1 半僵死循环同根因，**非"仅盘中心跳正常消失"**。

### ⑤ watchdog 断档复核 —— ✅ 与 Z2 交叉一致

- Z2 报告结论（同会话独立取证）：**无 17h 断档**；真实缺口仅 9/1 14:20-19:05（19:05 用户手动重启 Id 1074）+ 9/2 07:00-08:26（Id 41 "stopped responding/crashed/lost power" → 08:26:04 意外关机 Id 6008）
- 本报告独立交叉：watchdog.log 9/1 19:06-9/2 07:00 每 5 分钟连续记录（非断档）→ **一致 ✅**
- **23:50 TianjiGit_Nightly**：git 现 6 提交（含 `40f0c60 Z1 nightly fix: BOM encoding bug`）→ **Z1 已修复 nightly BOM bug，今晚 23:50 具备成功条件**（若本会话在场将盯梢，不在场按 §5 明晨命令验收）

### ⑥ 面板数据一致性 —— ⚠️ 部分陈旧（experiments 页）

| 数据面 | 实测 | 一致 |
|---|---|---|
| account.json | cash=82454.27，持仓 600121×2600（11:18 买入） | — |
| overview.time | 20:50:37（新鲜） | ✅ 不陈旧 |
| 指数 | 上证 3941.39(−0.97%) 等，time=20260902161402（收盘快照） | ✅ 与 9/2 收盘吻合 |
| regime | 崩溃(防守) max_pos=0 threshold=99 | ✅ 与大跌日匹配 |
| min5 | quality_alert min5_latest="2026-09-02 15:00:00" | ✅ 更新到收盘 |
| **experiments 页** | **generated_at=9/1 00:49:34，counts=161，今天新增未收录** | ⚠️ **陈旧数据实锤**（见⑦） |

Y5 提的"错误态/陈旧数据"：核心 overview 不陈旧（time 新鲜），但 **experiments 台账页陈旧**（索引 9/1 后未刷）——部分证实。

### ⑦ experiments_index 活性 —— ⚠️ 实锤：无自动调度，手动/重启才刷

- `data/experiments_index.json` mtime **9/1 00:49:34 未变**（95924B）；/api/experiments generated_at 同值、counts.experiments=**161**
- 生成器 `tools/experiment_scan.py`：**无对应计划任务**（schtasks 扫描无 experiment_scan 项）→ **非"每天跑"，靠手动/重启触发**
- **今天新增（pack9 验收报告、本次 Z 系列报告等）未被收录** → 实验台账页缺今日内容
- 建议（记录不修）：W5 应注册 experiment_scan 为每日任务或接入 watchdog 周期，否则台账永远滞后

---

## 2. 重大运行时事实（观察窗口内修正）

**当前活实例已非 8528**：

```
10:10:39  watchdog 拉起 8528（python main.py）
11:18     8528 买入 600121
11:22     8528 最后心跳 → 午后主循环未恢复
19:40-19:50 8528 overview 持续超时（audit_stale 15356s）
19:50:12  watchdog 判"半僵死 PID=8528"→ 终止 → 新PID=42508
20:05:16  42508 判死 → 43948
20:20:16  43948 判死 → 47096
20:35:16  47096 判死 → 44760（pythonw main.py，父=watchdog 49084）
20:40-20:50  44760 健康（l1 ok / overview time_age_0s / audit_age 855s）
当前       8899=44760，app.lock=44760（有锁，非裸奔）
```

**要点**：
1. **8528 已于 19:50 死亡**，当前活实例 = **44760**（20:35:15 起），**app.lock=44760 存在**（watchdog 半死恢复 clear_lock → 新实例写锁，锁机制在半死路径正常）
2. **半僵死循环 9/1 夜延续到 9/2 晚**：8528 午后卡死后，watchdog 每 15 分钟判死重启一次（42508→43948→47096→44760），每个新实例启动后 overview 立即超时 → 与 9/1 夜 39 次循环同模式
3. **Z2 报告"8528 为当前活实例、写回锁 8528"已过时**：8528 19:50 死亡后，Z2 的写回被 watchdog 半死 clear_lock 自然覆盖（锁=44760 现有效）。Z2 的锁消失机制分析（全死恢复不清锁 + main.py L104-109 缺陷）依然成立，但当前实例**非裸奔**（44760 有锁）
4. **根因指向**：引擎主循环在某点进入阻塞（午后 `_is_trading_time` 未恢复 / server overview 线程卡死），新实例启动也复现 → **需 Y2 查 overview 卡死与主循环阻塞点**（本批只记录）

---

## 3. 快照与 G7/P73 条件

- data/snapshots 现有：08-26 / 08-27 / 08-30 / 08-31 / **09-01** / **09-02**（15:34:15）+ _archive_pending_del
- **连续交易日快照 08-26 / 08-27 / 09-01 / 09-02 = 4 枚 → G7/P73 复验条件已满足**（任务书要求记一笔，P73 可解除挂起）

---

## 4. 明晨验收命令 + 23:50 nightly 盯梢

**今晚 23:50 TianjiGit_Nightly**（本会话在场则直接盯 watchdog.log/git log）：
```
# 盯 nightly 实况（23:50-23:55）
git -C 'C:\Users\26838\A股模拟盘' log --oneline -3
# 若出现 "auto 2026-09-03 ..." 提交 = nightly 成功（Z1 BOM 修复生效）
```

**明晨（9/3）验收命令**（若本会话已收工）：
```
Set-Location 'C:\Users\26838\A股模拟盘'
$env:PYTHONIOENCODING='utf-8'
# ① 台账：9/3 16:30 应见 LastRun 更新；若 ml_scores.json 仍 8/26 → 静默正常预期
Get-ScheduledTask ml_scores_ledger_daily | Get-ScheduledTaskInfo
# ② forward_eval：9/2 锚日 realized 应已结算
Get-Content data\forward_eval.jsonl -Tail 2
# ③ 当前实例与锁
Get-NetTCPConnection -LocalPort 8899 -State Listen
Get-Content data\app.lock
# ④ watchdog 半僵死循环是否终止（若 9/2 夜仍循环 → 半僵死 bug 未除，P1）
Get-Content tmp\watchdog.log -Tail 20 | Select-String '半僵死|恢复完成|健康'
# ⑤ nightly 提交
git -C 'C:\Users\26838\A股模拟盘' log --since=yesterday --oneline
```

---

## 4.5 盯梢更新（21:11）

- **44760 稳定**：20:35 起存活 36+ 分钟，watchdog 20:40-21:10 连续 7 轮全绿（overview time_age_0s），audit_age 线性增长（256→2053s）
- **半僵死循环在 44760 处暂时终止**（未再触发"判死-重启"；与 42508/43948/47096 各自 15 分钟即死不同，44760 盘后 overview 正常）
- 当前：8899=44760、app.lock=44760 有效、无第二实例风险
- ⚠ 需持续关注：8528 午后卡死的根因未除（overview/主循环阻塞），明日开盘（09:30）是 44760 或其后继的首次全时段压力测试

## 5. 证据与红线核对

| 证据文件 | 内容 |
|---|---|
| `data/ml_scores_ledger.jsonl` | 57 行未变（①） |
| `data/forward_eval.jsonl` | 9/2 锚日行 + 9/1 结算（②） |
| `data/reports/risk_daily/risk_daily_2026-09-02.md` | 20:00 生成（②） |
| `data/audit/audit.jsonl` | 11:18 strategy_buy、心跳分布、20:35 44760 启动（③④） |
| `app/trader.py` L307-370 | 心跳语义（④） |
| `data/experiments_index.json` | mtime 9/1 00:49 未变（⑦） |
| `tools/experiment_scan.py` | 生成器，无调度（⑦） |
| `tmp/watchdog.log` | 8528→44760 链 + 20:40-20:50 健康 |
| `data/quality_alert.jsonl` | min5_latest=9/2 15:00（⑥） |
| `data/snapshots/09-02` | 9/2 快照 15:34:15（§3） |

| 红线 | 状态 |
|---|---|
| 不改任何东西（进程/代码/数据/git） | ✅ 全程只读（Get-ScheduledTask/Get-Content/Invoke-RestMethod GET/只读 python/market.db URI ro） |
| Z1-Z4 交接=只读彼此报告 | ✅ 引用 Z2 结论交叉，未干预任何诊断 |
| 服务出事→记录+报告+等人类 | ✅ 8528 死亡/半僵死循环已记录，不自救 |
| 交付 `docs/reports/joint_obs_20260902.md` | ✅ |
