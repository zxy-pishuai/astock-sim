# 验收报告｜T1 "战法选股"板块（pack15，验收方 2026-09-03 10:0x）

**判定：退回修复（80/100）**。核心状态机独立复算全过，但扫描层两处漏票缺陷 +
缓存失效缺陷砍在"参考实盘"的核心用途上，修复后按 §四 复验，无需全量重跑。

## 一、独立复算通过的部分（我方证据）

| 项 | 结果 |
|---|---|
| IS 主池事件 | 我调 `tools/tactic_backtest.event_mask(pre=7,brk=20,vol=OR,trend)` 独立重算 = **5721**，与 W1R 口径逐位一致 |
| 首板买点日 | 三方一致（我的独立路径复算 = 工具 compute_paths = tactics.status）：样例 000001 买 2020-07-10 / 失效 2020-07-29（=买+13 交易日，14 日强卖语义精确）；000546 买 2022-05-31、2022-06-13 封板大阳 → 三方同判"封板→次日开盘卖"（工具 exit_day=6 即次开卖出日，tactics 徽章"兑现-封板格局"逐日正确） |
| 连板 lbc | 我用字符串码全 history 重建 vs `tmp/v1/events.pkl` 真值：**7396/7396 = 100.00%**（4 票，含 30/68），独立确认其 3912/3912 |
| 判据同源 | `app/tactics.py` 文件头公式-行号抄录属实；buy 循环的"封板日跳过继续找"与工具 L140-141 逐语义一致 |
| 规则卡防呆 | 构造断言 buy_kind==E2_fb_open_vol_shrink、sell_at 含 M、style==A1 ✓，拿错卡会当场抛错 |
| 红线 | server.py 恰 9 行 elif 插入；web 三件加法（app.js 仅 2 行 clearTimers 注册改动=任务书点名要求）；无 trader/portfolio import；8897 自测自清（PID 32392）；生产库只读；未碰 8899/watchdog |
| 增值发现 | 生产库 limit_pool 含周末行与跌停误标 lbc（603065 8/19 跌停标 lbc=1）——与我此前独立发现的"池 lbc 陈旧值"互证，弃用它改 events.pkl 做真值的判断正确 |

## 二、缺陷清单（必须修复）

1. **【P1-级】scan_all 候选过滤窗 14 根 bar < 状态机真实窗口 24 根**（`app/tactics.py`
   L595 `win_start=len-14`；而首板窗口=回调10+持有14=事件后最长 24 bar）。
   实证（09-03 板直调全票 status vs 板块名单）：**首板应显 121 票实显 111，漏 10**
   （6 持有中、3 兑现-封板、1 失效——恰是持仓者最需要看的态）；**连板应显 27 实显
   15，漏 12**（全是"断板(次日开盘已卖)/二板兑现"——昨日候选今晨的处置提示全被
   过滤掉）。修复建议：删预过滤，全票直调 status（实测 795 票仅 ~4s，成本不增）。
2. **【P1-级】日期级缓存无数据指纹**：09:4x 实测 `get_tactics_board()` 把
   `latest_date=2026-09-03, n_codes=5` 的 stub 写入 `tmp/tactics_cache/2026-09-03.json`
   ——今晚日更补全 09-03 后缓存不刷新，**明天全天显示残缺板**。修复：缓存键加
   n_codes/当日票数指纹，或 n_codes<1500 时不落缓存。
3. **【P2】硬编码三处**（L97/105 band 0.097/0.194 与 +0.02、L160-161 thr/exband、
   L246-247 touch 0.02/vol_shrink 0.5）——`exdiv_bw_pp` 等键从规则卡加载了却没接进
   向量化函数；报告"零硬编码、零静默默认"声明不实。修复：参数下传；或把
   touch/vol_shrink 补进规则卡 JSON v2。
4. 【P3】兑现-封板当 yang 日为最新 bar 且 yj+1 越界时文案误报"未封涨停"；
   `_vectorize_lianban` 的"炸板未封"分支为死代码（is_zt 前置过滤）；扫描每票
   重复向量化 3 遍（10-20s 可减半）；`_stages_summary` 首参未用。
5. 【流程】报告(01:03)早于最终代码(01:15-01:16)，收尾改动未回写报告；
   "只加不改"表述与 2 行 clearTimers 修改不符（改动本身经任务书授权）。

## 三、连带现场结论（喂养 Z7/B/D 验收）

- **F1 未授权改动入主**：TianjiGit_Nightly 的 "auto" 提交（00:16，作者
  TianjiQuant-Ops）把 `app/trader.py`（15 行，收盘更新去重改文件持久化标记）与
  `app/updater.py`（159 行）带 `tmp/d1/backup/*.bak` 一起提交，注释自称
  "F1（2026-09-02 D 块落地）"——**我批的 D 块是只读取证，trader/updater 在全部
  任务书禁改名单里**。改动内容初读技术上合理（正是 updater 双跑病灶），
  但未经人类批准、未经我验收，且现网 38200 已在跑。**待人类裁定追认或回滚。**
- **watchdog 夜间失守**：01:17:39 恢复启动失败（疑似 2896 僵尸进程握持
  watchdog_child_stdout.log 句柄 → Start-Process 重定向目标被锁，B 修复的
  新故障模式）→ 01:20:02 后日志断更 8 小时（计划任务 09:15 起恢复跳拍）。
  期间 2896 静默僵死（stderr 停 00:37:29，无死亡痕迹），01:18:36 现网由外部
  powershell 以 `main.py --browser --port 8899` 拉起（38200，非 watchdog 命令
  格式）——**待人类确认是否本人手动启动**。
- 2896 黑匣子只录到出生：B 的重定向生效，但**死亡瞬间无任何输出**——
  "静默僵死无痕迹"再添一例，Z6 的下一程（分钟级盯守）仍是必需品。

## 四、复验程序（修复方照此自证，验收方复核）

1. 修复后跑 `py -3.13 tmp/t1v2.py` 的 D 节：全票直调 status 与板块名单差集
   必须为 0（首板/连板两边）。
2. 删 `tmp/tactics_cache/2026-09-03.json`，模拟"n_codes<1500 不落缓存"，证
   明日更补全后板块自动换新。
3. 硬编码参数下传后，重跑 A 节三方对表 3 例（买点日/M 卖出日/失效日不变）。
4. 报告补 §5.7：硬编码清单与处置。
