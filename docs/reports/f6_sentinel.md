# F6（P2）票池同构性 / 数据质量自动处置 / ML 陈旧降级 —— 补做报告（2026-09-09）

规格书：`docs/reports/audit_20260908.md`（P1-2 票池不同构 / P2-4 停牌垃圾值 / P2-5 ML 陈旧 /
P2-6 复权跳变+长缺口）+ 任务书 F6（历史分桶 KPI、票池同构告警、复权跳变自动处置、长缺口剔除、
ML 陈旧降级为中性）+ 收尾①（amount=0 剩余 3,318 行构成）。

## 1. 落位改动（3 文件 + TTL 收尾，未提交，验收方统一登记）

| 文件 | 改动 | 行数 |
|---|---|---|
| `app/config.py` | +19：F6 开关 16 行（`UNIVERSE_EXCLUDED_FILE`/`MIN_HISTORY_BARS=250`/`HISTORY_INSUFFICIENT_AUDIT`/`ML_STALE_TDAYS=8`/`ML_STALE_FALLBACK`/`SUSPENSION_ROW_GUARD`/`GAP_EXCLUDE_TDAYS=250` 等）+ 收尾③ `LIMITUP_MEM_TTL_LIVE=120`/`LIMITUP_MEM_TTL_OFF=300` | 672→691 |
| `app/scoring.py` | +70：`_universe_excluded()` 运行时排除 + score_stock 历史门（max(60,250)）+ `ml_rank_bonus` freshness 降级（超 `ML_STALE_TDAYS` → 中性权重） | 902→972 |
| `tools/data_sentinel.py` | +179：新增 ⑦历史分桶+同构 KPI ⑧非物理量检测 ⑨指数完整性 ⑩当日缺票；`--out` 参数；ro 连接 immutable=1 | 480→659 |
| `app/limitup.py` | +3：120s TTL 魔数 → `C.LIMITUP_MEM_TTL_LIVE/OFF`（收尾③） | 389→390 |

全部 ast.parse + py_compile 通过、CRLF=0。备份：`tmp/f_backup/20260909_210444/`（9/9 一致）。

## 2. sentinel 扩展项实测（2026-09-09 21:1x 全量跑，475.6s，`tmp/f6_sentinel_test.json`）

| 检查项 | 实测值 | 判定 |
|---|---|---|
| ⑦历史分桶 | ≤100: **2876** / 101-300: 229 / 301-800: 99 / 801-2000: 427 / >2000: 1697（total 5328） | WARN |
| ⑦历史 ≥250 根 | 2422/5328（45.5%） | — |
| ⑦票池同构 KPI | 实盘池 5328 × 回测池 2422 → 交集占比 **45.46%** | WARN |
| ⑧非物理量 | 19 行（OHLC 四值全等占比 0.58；含 688432 09-07 volume=5.88e-37） | WARN |
| ⑨指数完整性 | high>=low=0 违规、amount_positive_pct=0（指数正常无 amount）、latest=09-08（**今日未更新**） | OK/WARN 观察 |
| ⑩当日缺票 | 09-09 vs 09-08：**缺 2,774 只**（today 仅 2,237 只） | WARN（实时异常） |
| 既有②复权 | 候选 1606 / high 330 / 未入排除 959 | WARN |
| 既有①缺口 | 2525/5253 有缺口；600165 缺 1773 天 | WARN |

## 3. 复权跳变自动处置 / 长缺口剔除（上轮 triage 产物，本批落位引用）

- `f6_qfq_auto_triage.py`（TEMP 产物）：77 只长缺口>250 日 → `data/universe_excluded.json`（运行时排除，
  保留数据打 `universe_excluded` 标记）；160 只 high 候选给出复权修复；92 只已处置。
- `scoring._universe_excluded()` 消费该清单——不再停在"由人工决定"。
- ⚠ 本批未跑 triage 写库（红线：本轮零写库；triage 写 `universe_excluded.json` 属 data/ 写入，留待获批窗）。

## 4. ML 陈旧降级

- `ML_STALE_TDAYS=8`（原阈值 25 过宽；实测 last_pred=08-19 距今 15 交易日 → 已 stale）。
- `scoring.ml_rank_bonus`：signal_date 超期 → 降级中性权重（不再沿用旧分）；audit 每笔买入记录 `signal_date`（F2 报告已有）。

## 5. 收尾①：amount=0 剩余构成（只查不清，2026-09-09 21:2x 实测）

任务书口径"剩余 3,318 行"无法精确复现（当前库非指数 amount=0 = **78,922 行**），按时段精确分解：

| 时段 | 行数 | 构成判定 |
|---|---|---|
| **<2026-05-18（历史回填段）** | **73,130 行 / 820 只** | **最大头 92.7%**。日期集中于 2024-12-16~2026-05-15（510300/688235/688234/688141 等 2024-12-30 起、指数 2024-02-29 起）——**扩表回填时 amount 字段未带**。volume 全 >0（**非停牌**）、票均为现役活跃票（**非退市**）→ 属"回填漏 amount" |
| 2026-09-07 | 2,504 行 | 当日更新 50% 票 amount=0（当日写入路径丢失 amount；其中 2,481 只在 09-08 已正常） |
| 2026-09-08 | 14 行 | 接近修复后残余（审计 P0-4 的 12 行） |
| **2026-09-09（今日）** | **2,233 行** | **实时异常**：今日 kline 全天仅 2,237 行（应 ~5,000）且 **100% amount=0**——今日收盘更新只写了 44% 且 amount 全 0，**需验收方优先排查今日更新链**（指数 latest 亦停 09-08） |
| 每日固定组 | ~8 只深票/日 × ~78 日 | 002728/002746/002748/002749/002797/002806/002937/003003 长期 amount=0（源/写入漏网）+ 3 指数（正常无 amount） |
| 05-18~09-05 回填后剩 | 1,041 行 | F3 回填 236,544 行后的漏网 |

**结论**：3,318 与任一时段不完全吻合（验收方统计口径含 09-07 当日 2,504 + 09-08 14 + 固定组 ~800 ≈ 3,318 最接近）。
本质构成 = **历史回填段漏 amount（73,130 行，非停牌非退市）+ 当日写入路径丢失（09-07 2,504 / 09-09 2,233）+ 固定漏网 ~8 只/日**。
无退市、无停牌占位（volume 全 >0；11 行非物理垃圾值另见 §2⑧，由 `f6_clean_garbage.py` 备份待批清洗）。

## 6. 短史票结论（任务书 F6 决策项）

- 新增 2,876 只 ≤100 根短史票，54.5% 池子无法计算 MA120/MA250/年线。
- **方案 A（继续回填至 ≥500 根）**：成本 = 全市场 × ~5 年 × 500 根日K 拉取（≈2,876 只 × ~400 交易日，
  按现速 ~250 只/分钟 ≈ 数十小时出网，且受限流约束），收益 = 回测池与实盘池同构（同构 KPI → 100%）。
- **方案 B（标记"仅实盘、不进回测"）**：成本 ≈ 0，立即消除同构失真；收益 = 回测结论可信（45.5%→100% 交集
  只影响实盘新增票的覆盖，回测池固定 2,422 只）。
- **推荐 B 先行**（min 改动、立即止血），A 作为后续批次的回填项目；`MIN_HISTORY_BARS=250` 已作运行时过滤底线。

## 7. 红线遵守

零写 market.db；零出网；未碰进程/未重启；备份+manifest 9/9；config 只新增不改既有值；`tools/service_watchdog.py`、`app/trader.py`（情绪段除外，见 F2 报告）未越界。
