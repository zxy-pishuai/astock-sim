# 验收报告｜R2 台账执行 + E2/E3/E4 + E1 复核（2026-09-04 23:2x，末班验收）

- 方法：逐项读码核验（grep 定位+关键段阅读）+ 交付报告证据链复核 + 生产库/审计实测。
- 说明：本报告为交班前末班验收；后续由接任验收方按 `tmp/handoff/` 两份文档接管。

## §1 总判定

| 块 | 判定 | 分 | 一句话 |
|---|---|---|---|
| R2 台账执行（P0×3+P1×4） | ✅ 通过 | 93 | 七项修复全部在码，备份齐全，待重启生效 |
| E2 watchdog 重定向唯一名+降级 | ✅ 通过 | 94 | 铁律落实，句柄锁模拟测试通过 |
| E3 连板 20cm 堵口+拆分 | ✅ 通过 | 95 | 堵口实证、正确性门复现、顺带修复 V1 按年索引 bug |
| E4 C1 §2.3 条件债 | ✅ 通过 | 90 | 数字可复现+配置分叉说明+追加证据 |
| E1 日K全库修复 | 🟡 未完成 | 70 | 代码落地但回补半途而废、报告未交、发现盘中 bar 风险 |

## §2 R2 七项逐条读码实锤

1. **P0.1 板块首刷**：`tactics.py` `_vectorize_both`(L205) 被两卡共用
   (L429/L650)；`_prev_trading_cache`(L1090) + `_RESCAN_LOCK` + daemon Thread
   后台重扫(L1117-1178) + `stale_ok` 语义齐备；`idx_kline_pd` 已入
   sqlite_master（实测）。✓
2. **P0.2 覆盖告警分级**：`updater.py` L789-791 <0.8 CRITICAL / <0.9 WARN；
   `missing/missing_sample` 随事件落 audit；快照事件带新鲜度+带病 WARN
   (L880-881)；前端红条 ratio<0.9（app.js）。✓
3. **P0.3 watchdog 心跳**：`service_watchdog.py` HEARTBEAT_FILE+touch(L68-213)；
   `trader.py` `_check_watchdog_heartbeat`(L204-217) 超时记 CRITICAL
   watchdog_stale，主循环已挂接(L303)。✓
4. **P1.4 limit_pool 周末门**：写侧 L66-70 + 读侧 L91-94 双门；库内实测
   limit_pool=1128 行（清 272 条后）。✓
5. **P1.5 锁重建**：`main.py` 僵死清理 `os.remove` 后重写 PID（读码确认）。✓
6. **P1.6 audit 轮转**：`audit.py` `_maybe_rotate` 50MB(L22/L102-108/L151)。✓
7. **P1.7 竞价快照主动抓**：`trader.py` 09:26-09:39 主动 `auction_board()`，
   每日去重 `_auction_snap_ts`(L143/L241-253)。✓

扣分：台账"服务在跑 44104"系笔误（实测属主 16804，−2）；生效依赖重启，
台账已如实声明（不扣）。**生效提醒：以上全部等服务下次重启；验收方不重启。**

## §3 E2/E3/E4

- **E2**：唯一文件名 `watchdog_child_<kind>_<ts>_<seq>.log`(L324-336)；
  start_service 失败→记日志→**无重定向重试**，句柄锁模拟测试输出在案。
  固定名时代终结。✓
- **E3**：板块堵口五处改动（status 入口/yest_candidate/today_lbc==1/
  观察档 gem_note/梯队），3 只历史 20cm 票实测全部"仅观察"灰系、
  10cm 对照票不受影响；拆分回测 `board_split` 三档+参数校验；
  **正确性门**：混合 all IS n2709 +2.562% vs V1 n2709 +2.56% 复现 ✓。
  结果：10cm IS +2.543%/OOS +2.211%（8 年逐年正，n 307-1276 可信）；
  20cm n=41/82（按年 7-36）判**不可信**——维持 10cm 限定，诚实。
  **加分**：顺带发现并修复 V1 `main()` 按年循环索引错位 bug
  （`c_buy[i]` 子组内索引错取全量数组）——**推论：V1 报告按年表由带病
  循环产出，以 E3 修复后的按年数字为准**（总量不受影响，正确性门已证）。
- **E4**：§2.3 数字改为交付缓存可复现值（72.9840→24.5730 / 45.8970→84.8450），
  复现脚本 `tmp/e4/repro_2_3.py`，配置分叉（P2 抽样 parts 缓存 vs 全量重跑）
  说明清楚，另补 §2.4/§2.5 证据。条件债清零。✓

## §4 E1 复核（🟡 未完成，移交清单）

1. **代码已落地**：`update_daily` stale_first（预算 480s/批 250/旧签名兼容）
   + `_daily_coverage` KPI——读码确认 ✓。
2. **回补半途而废**：9/3 23:41 起跑 5 批（约 1000 票）后过夜停摆，
   9/4 13:16 续 1 批后 **BUDGET-EXHAUSTED**——日志显示"已用 48941s"，
   **预算把夜间停摆的墙钟时间全算进去了**（实际干活约 3 分钟）。
   净成果 1197 票回补（宇宙 5328），历史覆盖升至 ~1900-2000。
3. **现网仍跑旧码**：今日 15:14 收盘更新 daily_updated=**300**、
   无 daily_coverage 事件——服务 16804（9/3 11:35 起）未重启，
   E1/R2 代码均未生效。今日 9/4 覆盖 783/2526。
4. **新发现风险（P1，移交）**：13:16 批在**盘中**写库，最多 ~200 票的
   9/4 日K是截至 13:16 的**盘中残缺 bar**；stale-first 以 max(date) 判新鲜，
   这些票会被视为已追平而**永不回补**。建议：重启生效后做一次定点重拉
   （对象=回补日志 13:16 批的 200 票，days=5 覆盖写），或给回补加
   "盘中跳过当日 bar"守卫。
5. **报告债**：`e1_daily_fullcov.md` 至今未交。

## §5 移交备注（给接任验收方）

- 用户重启服务后第一班：核 stale-first 生效（明日收盘 daily_updated≫300）、
  daily_coverage CRITICAL 事件、覆盖逐日爬升、竞价快照主动抓取、板块红黄条。
- 9/3 与 9/4 快照均带病（覆盖缺口），涉及回测打折看。
- 今晚 23:50 git nightly 应收走全部未提交改动（含本报告），明晨复核。
- 其余线索见 `tmp/handoff/project_bible_20260904.md` §8。
