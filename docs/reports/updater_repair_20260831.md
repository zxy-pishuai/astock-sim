# 数据通道修复报告 — 2026-08-31（P0 收盘增量归零 · 独立交叉验证轮）

- 时间: 2026-08-31 00:40 落盘（诊断期 00:20-00:40，低峰只读）
- 角色: T2 任务2「数据通道修复」；本报告为 **8/30 已交付报告（`updater_repair_20260830.md`）的独立交叉验证轮**，并纳入 8/31 交易日视角
- 只读取证: `data/audit/audit.jsonl` + `data/update_state.json` + `market.db`/`min5.db`（URI 只读）+ `data/snapshots/` + `data/quality_alert.jsonl` + 12 只样本 5 通道拉取复现（只拉不写）
- 约束: 未改任何 `.py`；未动 `app/config.py`（红线1）；未手写 `market.db`（红线2）；未注册计划任务；未盘中压测（00:2x 低峰 ≤12 只）；`py_compile`/LF 不涉及（零代码改动）

---

## 0. 判定先行（给急着看结论的人）

**根因（8/30 报告已定，本报告独立复核一致）：** 三次失败（8/21、8/24、8/28）均为 **收盘后 15:10 首触发窗口内，全市场并发撞上上游按 IP 限流/服务端过载**，非我方代码退化。证据：当前（8/31 00:30 低峰）TDX/腾讯/新浪 **5 通道 12/12 全部成功**，8/30 22:08 同代码低峰自愈并产出快照。

**当前状态（8/31 00:31 实测，已自愈）：**
- 服务运行中（PID 67120，端口 8899 监听）；
- `update_state.json → {"last_day":"2026-08-28"}`；
- `kline day MAX=2026-08-28`（702 行）、`min5 MAX=2026-08-28 15:00:00`；
- 快照最新 `data/snapshots/2026-08-30/market.db`（1299.9 MB, rows 8693312, max 2026-08-28）；
- 哨兵 `data/quality_alert.jsonl` 末条 `2026-08-30T22:09:45`（min5_latest 正确）。

**今日关键验证点：** 今天是 **2026-08-31 周一**（8/30 报告把 8/31 误记为周日，本节已纠错，见 §5）。今日 15:10-15:40 收盘窗口将触发下一次收盘增量更新——**成功判据的验证窗口就在今天**（联合观察窗 15:30-18:30，服务内 updater 为唯一变量）。

---

## 1. 本报告定位

8/30 22:40 已交付 `docs/reports/updater_repair_20260830.md` + `data/updater_diag.json`（47 条时间线、三次失败复盘、样本复现、根因三档、修复建议）。按协调协议「共享状态一律自跑只读证据、不得凭对方声称完成下结论」，本报告对 8/30 报告**全部关键数字独立复核**，结果见 §2；并补充：日历纠错（§5）、触发机制代码级证据（§6）、今日验证窗口（§8）。

---

## 2. 独立交叉验证（8/30 报告 vs 本报告自跑证据）

| 项目 | 8/30 报告断言 | 本报告独立实测 | 判定 |
|------|--------------|----------------|------|
| 三次失败时间线 | 8/21 16:01:13、8/24 16:01:10、8/28 17:08:07，均 min5 0/全 empty + daily 0/300 | 审计链逐条过滤一致（§3 表） | ✅ |
| daily 300 分母 | `get_stock_list()[:300]`，非宇宙 5009 | `update_daily L256 limit=300` 代码确认；stock_list 5009 | ✅ |
| 8/28 反例 | 410→702 行系 300 切片外路径补库 | kline 8/28 实测 702 行（>300），300 切片与全表口径不同不矛盾 | ✅ |
| min5 empty 语义 | "拉到但空"非报错（errors=0） | `_fetch_tx_min5` 返回 [] 计数 empty；三次失败 errors 均 0 | ✅ |
| 根因=上游限流 | 时间戳对齐+双通道同零+清单放大+相邻日成功 | 本报告 §4 复述一致 | ✅ |
| 快照 | 2026-08-26/27/30 三个 | 目录实测 2026-08-26/27/30，大小一致 | ✅ |
| 新鲜度 | kline max 8/28、min5 max 8/28 15:00、state=8/28 | 独立查询一致 | ✅ |
| **日历** | **08-30 周六、08-31 周日、09-01 周一** | **实测 08-31 周一（§5）** | ❌ 需修正 |

除日历表述外，8/30 报告结论**全部通过独立复核**。8/30 版 `data/updater_diag.json` 已备份为 `data/updater_diag_20260830.json`，`data/updater_diag.json` 现为 8/31 版。

---

## 3. 三次失败 vs 相邻成功（浓缩时间线）

| 日期 | 触发→完成 | min5 | daily | 备注 |
|------|-----------|------|-------|------|
| 8/21 16:01:13 | 15:10→16:01 (~51 min) | 0/496 empty 496 | 0/300 | 首例 P0 |
| 8/24 16:01:10 | 15:10→16:01 (~51 min) | 0/496 empty 496 | 0/300 | 同日 09:48 曾 496/496 成功 |
| 8/28 17:08:07 | 15:10→17:08 (**118 min**) | 0/1004 empty 1004 | 0/300 | P62 后清单 1004，耗时翻倍 |
| 8/25 15:11:14 | — | 496/496 | 300/0 | 同 15 点档成功（关键反例） |
| 8/26 15:14:46 | — | 999/1002 empty 3 | 300/0 | 15:11 首触失败→3 min 后重试成功 |
| 8/27 15:18:11 | — | 999/1002 empty 3 | 300/0 | — |
| 8/30 22:08:22 | 重启后 ~6 min | **1001/1004 rows 48592 empty 3** | **300/0** | 低峰自愈，同代码 |

---

## 4. 根因证据链（浓缩）

1. **收盘后集中并发撞限流/过载（首因）**：三次失败均为收盘后首触发完成窗口（51-118 min vs 成功 3-10 min）；同日 min5 `empty==total` 且 daily `errors==300` 双通道同归零，总量随 P62 清单 496→1004 放大；相邻成功日同代码同链路非高峰全成功（backlog P62 已知按 IP 限流）。
2. **TDX 6 连接池 vs 8 并发挤占（放大器）**：`tdx.py _POOL_SIZE=6` vs `updater.py PARALLEL_WORKERS=8`，低峰同池成功证明池本身健康。
3. **代码退化（排除）**：当前 5 通道 12/12 全通（§4.1），8/30 22:08 同代码成功。
4. **非"数据未发布"**：若未发布，8/25 15:11 同 15 点档也应空，实为成功；且失败日 `kline day` 当日仍有部分入库。

### 4.1 样本拉取复现（8/31 00:30，12 只，只拉不写）

用项目真实解释器 `C:\Users\26838\AppData\Local\Programs\Python\Python313\python.exe`（⚠ 勿用 Doubao 沙箱解释器，缺 mootdx 会产生 tdx 假阴性）：

| 通道 | 结果 |
|------|------|
| TDX 日K (qfq, 80) | 12/12，80 行/只 |
| TDX min5 (20) | 12/12，20 行/只 |
| 腾讯日K (80) | 12/12，79-80 行/只 |
| 腾讯 mkline min5 (20) | 12/12，20 行/只 |
| 新浪日K (80) | 12/12，80 行/只 |

`tdx.available()=True`，mootdx 已装。失败签名（empty=全/errors=300）在当前低峰**不可复现** → 失败为收盘后高峰窗口的临时性上游限流/过载，非代码缺陷。

---

## 5. 日历纠错（重要，影响验证窗口）

- **8/30 报告错误**：`updater_repair_20260830.md` §5/§8 写"08-30 为周六、08-31 为周日、09-01 周一 15:30 后自动更新"。
- **实际日历**（系统实测 2026-08-31 为星期一）：08-28 周五、08-29 周六、08-30 周日、**08-31 周一**、09-01 周二。
- **影响**：8/30 报告把成功判据的验证窗口错推到 09-01；实际**今天（08-31 周一）15:10-15:40 收盘窗口即触发下一次收盘增量更新**，验证窗口就在今天下午。
- **不受影响**：8/30 快照产出、`update_state=08-28`（周末 `target_day` 归 08-28）等结论方向正确。

---

## 6. 触发机制缺陷（代码级证据）

- 收盘触发点：**`app/trader.py L258-266`**，条件 `1510 <= hm <= 1540`，每日一次（守卫 `today != _last_data_upd`）。
- **缺陷**：窗口单次、`_last_data_upd` 置位后**当天不再重试**；`start_background_update`（updater.py L700）只在**服务启动**时由 `needs_update()` 兜底重试。
- 恢复证据：8/21 失败 → 8/22 21:00 才有下次 data_update；8/28 失败 → 8/30 22:02 重启 → 22:08 自动补跑成功。→ "失败后 1-2 天或重启才自愈"的结构性根因。

---

## 7. 修复建议（只建议，不执行、不注册任务）

1. **错峰/退避**：收盘触发点（trader.py L259）从 `1510` 延后至 `1530` 后，或加"当日数据就绪探测"（先探活单票 600000 日K+min5，失败则 10 min 后重试）；`PARALLEL_WORKERS` 收盘档降并发（backlog P62：16 并发更慢）。
2. **指数退避**：`update_daily/_one` 对 429/503/timeout 做指数退避（可推广 `_g_em_close` 既有模式）。
3. **达标重试门**：`run_update` 对 `daily` 设 `updated>=250` 重试门（当前仅 min5 达标才推进 `last_day`）。
4. **分片轮换**：1000+ min5 清单隔日半片轮换，降低单次并发总量。
5. **失败可观测**：`run_update` 的 except 中补 `audit.record(event=source_health, http_status=...)`，避免当前仅聚合计数、无 HTTP 状态可复盘（8/30 报告 §9 同提）。
6. **告警阈值**：`data_sentinel.py` 已有 `check_ml_pred`（ML_PRED_STALE_TDAYS=25）；对 updater 失败日可加"连续 N 日 last_day 未推进即 WARN"的观察（写入报告建议，不改代码）。

---

## 8. 成功判据与今日验证窗口

| 判据 | 期望 | 今日实测锚点 |
|------|------|-------------|
| `daily_errors < 5%` | 300 中 <15 | 待今日 15:10-40 触发后核验 |
| `min5_updated > 0` | 真实入库 >0 | 待核验 |
| 新快照自动出现 | `data/snapshots/2026-08-31/market.db` | 待核验（auto_snapshot 更新成功后冻结） |
| 哨兵当日 thin 全 false | 当日条目 | 待核验 |
| `update_state last_day` | `2026-08-31` | 待核验 |

**验证窗口**：今日 15:30-18:30 **联合观察窗**——服务内 updater 为唯一变量，双方禁一切手动触发；16:30 台账 tick、20:30 forward_eval 由 T1 观察。若今日触发仍失败 → 按 §6 缺陷确认"当日不重试"，需人类批准二次窗口（15:00-15:29 或次日）手动 `updater.run_update()` 重试。

---

## 9. 未解/待观察

- **今日 15:10 收盘触发结果**为本任务最终成败判据，尚未发生（报告落盘时刻 00:40）。
- `ml_pred MAX=2026-08-19` 独立陈旧（`ml_sidecar` 离线，非 updater 管理，属任务5 范围）。
- 哨兵 `high_severity 260 / candidates 1437` 为 P71 qfq 全量候选，非本次增量引入。
- 高峰 16:01 的真实 HTTP 状态（429/超时体）未落盘（audit 仅聚合计数）——已列入 §7.5 建议。

---

## 10. 批外变更检查与交付物

- **批外变更检查（红线1）**：`config.py` mtime 2026-08-27 07:30:18、`updater.py` 2026-08-27 02:52:43、`datafeed.py` 2026-08-26、`tdx.py` 2026-08-19、`data_snapshot.py` 2026-08-25、`main.py` 2026-08-26——**均早于作业窗，无批外变更**。`tools/act12_t4_pit.py`（8/30 23:56）属任务4 交付物，未触及数据通道代码。
- **改动 diff 摘要**：本批 **零代码改动**（根因为上游临时限流，8/30 22:08 已自愈，硬修无收益，符合任务书"非我方代码不硬修"）。
- **交付物**：
  - `docs/reports/updater_repair_20260831.md`（本报告）；
  - `data/updater_diag.json`（8/31 版：独立验证/样本复现/日历修正/触发机制/成功判据）；
  - `data/updater_diag_20260830.json`（8/30 版基线备份）；
  - 协调日志 `tmp/coord_t1_t2.log`（本报告签署行）。
- `docs/backlog.md`：本批无代码改动、无新阶段，无需更新（与 8/30 报告一致）。

---

## 附：协调协议会签（T2）

- `2026-08-31T00:45 | T2 | OBS_DONE | 任务2 只读诊断完成：8/30 报告全部数字独立复核一致（除日历错误）；5 通道 12/12 全通；今日 15:10-40 收盘触发为成功判据验证窗口`
- `2026-08-31T00:45 | T2 | DISPUTE | 8/30 报告日历错误：称 8/31 周日、9/1 周一；实测 8/31=周一，今日 15:10 收盘触发即验证窗口`

---

## 附：备份声明勘误（2026-08-31 C块）

**勘误声明**：本报告交付物 `data/updater_diag_20260830.json`（2061B）的 `backup_note` 声称“内容与 2026-08-30 22:40:41 生成的 data/updater_diag.json 完全一致”——**该声明不实**。实测该文件为 8/30 原文件（24492B）的**摘要**，仅保留 `timeline_all_count=47` 等核心结构，未包含 47 条 `timeline_all` 列表本身；其 `note` 所指“完整 47 条见 docs/reports/updater_repair_20260830.md（该文档含完整表格）”亦不实——该报告 §1.1 仅失败/相邻成功 8 行对照表，无 47 行全量时间线表格。

**完整版**：47 条时间线已从审计链只读重建，见 `data/updater_diag_20260830_restored.json`（每条附审计链行号 `_src_line` 自证来源）；勘误取证详见 `docs/reports/diag_forensics_20260831.md`。
