# 双价观察周报（P59 buy_px_dual，新旧口径差异跟踪）

- 工具：`tools/dual_price_weekly.py`（每日事件流增量，周度汇总；`--selftest` 自检聚合口径）
- 口径预注册：bps=(新−旧)/基准×1e4；不利方向=买侧新更贵/卖侧新更贱；金额差仅对带 qty 子集可估，符号统一为"正=新口径对交易者不利"
- 判定归验收方：建议以"整周样本≥30 且 不利中位幅度≥5bp 或 不利金额累计超佣金两个数量级"作为启动切换评审的参考线；`BUY_PX_USE_NEW` 目前未在 config 定义（getattr 默认 False）
- 运行方式：`python tools/dual_price_weekly.py`（dry-run）/ `--write`（落盘本表与 data/dual_price_weekly.json）；周度批处理 `tools/dual_price_weekly.cmd`

## 首跑分析（D4 dry-run，2026-08-27）

**存量：0 条事件。** 判定为"无定价发生"，非"信号被吞"，依据：

1. **采集器健康**：audit.jsonl 共 1,875 行、0 坏行，覆盖 2026-08-16 07:26 ~
   2026-08-27 02:22，心跳连续——事件通道本身工作正常。
2. **发射路径未触发**：双价只在策略自动买入（trader 扫描 L634 / 竞价板 L848）
   与策略退出卖出（L1254）定价时 emit；观察期内这些路径 0 次执行
   （order 类仅有 manual_buy×5、test_buy×1、演练×3）。manual/test 路径在
   `state.py` 自行定价、不经过 `_buy_px`，天然不入观察流。
3. **等价静默语义**：新旧两价的分叉充要条件是「真实 qty 已知 且 当日成交额>0
   且 单笔金额/当日成交额 >5%」（冲击成本项）；其余情形两价恒等、不写日志
   （防刷屏设计）。因此即使有单，也只有薄票大单才会进入本表。

### 观察期证据流的已知局限（写给切换评审）

- **覆盖缺口 A（历史）**：dual 记录自 P59 部署才存在，此前成交无法回溯补记；
  观察从零开始积累。
- **覆盖缺口 B（旁路）**：manual_buy/test_buy 不走 `_buy_px`，若这两类占实盘
  下单比例可观，应评估是否也接入双价记录（属后续改动，本轮不动代码）。
- **证据链健康度警告**：容忍式全扫发现 audit 哈希链断点 **339/1875**
  （重同步口径）——多进程并发写（服务+工具+计划任务各自持有内存链头）与
  同日重启都会断链，集中爆发于 08-20（67 处，断源演练日）与 08-26
  （253 处，P71 排除扩容落地日）。单条 dual 事件内容仍可读可信，
  但"链级防篡改保证"在并发写场景不成立；如需强证据流，建议后续改造
  `app.audit.record` 为写前读文件尾作 prev（每条 O(小) 开销），本轮未动。
- **qty 缺省记录**：qty 缺失的事件（退回 ×1000 假设）bps 可算但金额不可估，
  工具按子集单独列示。

| 周 | 事件数 | buy侧（数量/中位/P90/max|bps|/不利%/估额） | sell侧 | 触及代码 | adopted |
|---|---|---|---|---|---|
| （尚无样本：见上方首跑分析；事件累积后由工具自动追加行） | | | | | |

<!-- 本次运行 2026-08-27 04:08:39：0 条事件（含历史累计重算，幂等去重复述） -->

<!-- 本次运行 2026-08-27 10:44:55：0 条事件（含历史累计重算，幂等去重复述） -->

<!-- 本次运行 2026-08-30 20:25:56：0 条事件（含历史累计重算，幂等去重复述） -->

<!-- 本次运行 2026-09-06 09:00:02：0 条事件（含历史累计重算，幂等去重复述） -->

<!-- 本次运行 2026-09-13 09:00:01：0 条事件（含历史累计重算，幂等去重复述） -->
