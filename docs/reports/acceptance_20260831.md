# 2026-08-30 批（P78–P82）验收报告 —— 任务1/2/3/5（任务4 在跑，本批不验）

- 验收时间: 2026-08-31 00:00–00:15 ｜ 验收方: ox-alpha ｜ 纪律: 只读，唯一写入 = 本报告
- 验收依据: tmp/task_brief_20260830_v2.md 各任务判据 + 六条通用红线
- **总判定: 任务1 ✅ / 任务2 ✅ / 任务3 ✅ / 任务5 ✅（各带备注级瑕疵）；
  另发现 1 起批内数据事故（C 批历史结果 JSON 被覆盖损毁，见 §5，处置方案已给）**

## 0. 验收时系统状态

- 服务 8899 **LISTEN**，PID 67120，CreationDate 2026-08-30 22:02:25（任务1 成果，在线）
- data/app.lock 存在；audit.jsonl 2671 行；breaks 340（末位行 2124，其后零新增 ✓）
- 快照三件套: 08-26 / 08-27 / **08-30**（1299.9MB，manifest rows 8,693,312，22:08:43 自动产出）
- update_state.json = {"last_day": "2026-08-28"}（周日 target_day 语义正确，tcal 实测）
- 日历勘误: 实测 8/29=六、**8/30=日、8/31=一（交易日）**——任务2 报告 §8 注写
  "8/30 周六、8/31 周日、下步观察 9/1"的星期标签错位一天；其 target_day=08-28 的
  结论仍正确（8/30 非交易日），但**下一次收盘自动更新在 8/31（今天）15:10，不是 9/1**。

## 1. 任务1｜服务恢复与热修复生效 —— ✅ 通过（92 分）

复核为真的关键证据:
1. 重启事实链完整: 端口 8899 LISTEN/PID 67120 与 CreationDate 实证在线；
   app.lock=67120；startup_check 退出码 0 全文粘贴（①生效参数全过、影子线程
   启动证据在 /api/log、前瞻台账 8/28、进程新鲜度 core mtime ≤ 实例启动）。
2. 审计链: --verify 前后 breaks 340→340 零新增；新行（08-30 首条 prev=""，
   hash 重算匹配）；末位断裂仍为 2124；tmp/breaks_before*.json 双留底实存（76181B）。
3. 三热修复: pos_cash 顺序（inspect+源码 L665 注释双证）、单写者（新行零断裂）、
   sector 缩水保护（_shrink_guard L158 在位 + 70%/80% 单元对测）+ sector_map
   实测 2911 键/49 行业/0 坏格式 ✓。
4. 副作用声明规范: audit.jsonl +1 自写行、breaks 存档刷新均如实披露。
5. 遗留观察表（双价/归因/收盘增量）基线化清楚，判据可执行。

瑕疵（备注级）:
- §1e 出现一处韩文乱码"이후"（应为"之后"），内容无损；
- **§5 观察项星期错位**："8/31(周日)无交易日跳过、09-01(周一)收盘后验证"——
  实测 8/31=周一交易日，**关键验证就在今天下午**，与任务2 犯同一错误（见 §2）；
- **批准重启声明"已获人类批准（ask_user_question）"验收方无法独立复核，
  需用户确认自己确实在 ~22:01 批准过**（若未批准 → 升级为阻断项）；
- sector_map.json 22:03 被服务预热重写（键数不变）未列入副作用声明，属披露小漏。

## 2. 任务2｜数据通道修复 —— ✅ 通过（88 分，本批质量最高）

复核为真:
1. **自愈全链实证**（验收方独立重查）: audit 22:08:22 data_update 事件
   min5 1001/1004 rows 48592 empty 3 err 0 + daily 300/0 ✓；22:08:46
   data_snapshot 事件 + data/snapshots/2026-08-30/market.db 实存（1299.9MB，
   manifest 与报告逐字一致）✓；kline 8/28 现 702 行、min5 MAX 8/28 15:00
   distinct 1001 ✓；update_state last_day=08-28 与 _target_day 周末语义一致 ✓。
2. **三次失败时间线**与审计链全量核对一致；"daily_errors=300 分母=
   update_daily limit=300 切片"的定性与代码实证相符，并把我任务书里
   "410 行反例"正确解释为失败时刻中间态（现 702 行，口径澄清到位）。
3. 根因判定（收盘后并发撞 IP 限流为主因、TDX 池 6<8 并发为放大器、我方代码
   可排除）证据结构合理；"同代码低峰自愈"是当前最强证据。
4. **未硬修 .py 符合任务书分支**（非我方代码 → 不硬修，交升级诊断+退避建议）；
   config.py mtime 未动（8/27 07:30 原值）✓；红线零违例。
5. updater_diag.json 结构完整（timeline_all 47 / hypotheses_ranked /
   daily_300_denominator 等 10 键）✓；LF、JSON 可解析 ✓。

瑕疵（备注级）:
- 星期标签错位一天（§8 注），连带"下次观察 9/1"表述错——**正确时点是
  8/31（今天）15:10 收盘自动更新**，联合观察协议仍按原窗口 15:30–18:30 执行；
- §5 全球行数 DINIW 写 3111，实测 6,943（P35 已回补过，引用了旧数）；
- 调试脚本 tmp_diag1.py 落在**项目根**而非 tmp/（1349B，只读查询性质，无危害，
  违反临时文件归置惯例）；
- 协调日志 tmp/coord_t1_t2.log 双方都未创建使用（协议未执行）——本次因
  "T2 无补丁 + 观察自然错峰"未造成实害，但协议合规性记 0。

## 3. 任务3｜P77 收尾 + P73 + 择时取证 —— ✅ 通过（90 分）

复核为真:
1. **P77 重算全对**（验收方独立按 judge_kit.landing_rule 语义复算四臂）:
   T2 loss=[31.23,6.66,4.05,22.58] windows_ok=0 fail ✓、T2_A1 ok=1(牛0.75)
   fail ✓、T2_A2 ok=1 fail ✓、两 T3 复用臂 ok=3 pass ✓ —— 与报告 A.1
   "按 prereg 复核"列逐位一致。
2. **抓到 P77 原落盘脚本的判定符号病并如实披露**（JSON verdicts 的 windows_ok
   用 `loss>=-1` 反号口径致 T2_A1 被误记 pass、recommendation"建议落地"与
   预注册相反；正是 judge_kit.py L30 记录过的历史教训复发）——按任务书
   "以 prereg_judge 为准 + 记录不一致"处置，方法论正确、影响结论修正有效。
3. PIT 单格 overstatement +55.16pp 与 baseline_pit|pit 数据相符；"仅牛市格可
   双列"如实声明 ✓。backlog L31 补了 P77 行 ✓（含结论摘要）。
4. P73: 快照 3/3 就绪属实（08-30 新增），干跑通过叙述与脚本"需≥3"逻辑一致；
   未执行 --execute 属"就绪但未收割"——**留整改项**。
5. 择时取证: 全库 index_timing/sentiment 事件 **0 条**（验收方独立扫描 2671 行
   复核 ✓）；config L209/L215 两开关 False + 消费面 trader L619/L765、
   engine L128 接线核对 ✓；backlog 风险看板第 2 条已追加取证结论（L41）✓。

瑕疵（备注级）:
- §C.2 "mtime app/config.py 2026-08-26 07:47 早于回滚"表述错误——
  config.py 实测 mtime 是 8/27 07:30:18（002594 移出那次），8/26 07:47 是
  L215 注释里的**回滚决定日期**，两者被混写（结论"未重开、当前 False False"
  不受影响）；
- §B.0 对同一事实两处 mtime 表述互相矛盾（207-208 段写 8/27、C.2 写 8/26）。

## 4. 任务5｜ml 侧车链诊断 —— ✅ 通过（86 分；异风格 AI，按数值逐条复核）

风格差异（口语化、"先看这个"式开头、含环境限制自述）不影响实质——
**全部关键数字独立复核为真**:
- ml_pred MAX=8/19、1,411,194 行、1,693 只、尾部 899→829→776 逐日 ✓；
- ml_scores.json date=8/26、1700 分、mtime 8/27 04:26 ✓；
- 台账 54 行（pick 40 / ret 14）、登记信号日仅 {8/26}、1/20 ✓；
- 覆盖率基线 424/700=60.6%（R2 246/R3 30）与生产文件 reason_counts 逐项 ✓；
  重跑 341/700=48.7%（R2 359、R3 0）与 TEMP 副本 ml_coverage_diag_rerun_
  20260830.json 逐项一致 ✓；生产基线未覆盖（94848B、8/27 02:43）✓；
- selftest 复现数字与 ml_scores_ledger_b4.md §4 记录完全一致 ✓；
- 21 交易日≈29 自然日的最短路径测算 ✓ 成立。

亮点: 发现 `scores_ledger.conn_ro()` 普通 mode=ro 在服务运行时报 disk I/O
error（真实缺陷，只按红线给建议未改码）；诚实声明其 shell 通道对项目目录无写权限、
产物经其正规写通道落盘——环境差异如实披露，无粉饰。

瑕疵（备注级）:
- §0"服务自 8/28 后收盘更新未跑"写于 23:17，未反映同日 22:08 已成功一轮
  （其数据截止表述"kline 停在 8/28"是对的，因果叙述过时一天）；
- 建议把 data_sentinel ML_PRED_STALE_TDAYS 25→5 未同时提醒"predict 若日频化，
  ml_pred 周频 + ml_scores 日频的两级新鲜度要分开阈值"（可接受的小疏漏）。

## 5. 🔴 批内事故：C 批历史结果 JSON 被覆盖损毁（阻断级，与任务4 在跑进程相关）

- 事实: `data/bt_c_lane_redispatch.json` 于 **2026-08-30 23:29:12 被重写**，
  44 格 runs 全部变成 `BrokenProcessPool` 错误记录、elapsed_s=61.5、
  judgement 全 missing-cell——8/27 原运行 44 格数值在 JSON 层**已丢失**。
- 嫌疑机制（推断，非定论）: 任务4（复用 c_lane 配方）的**第一次尝试**复制脚本后
  OUT_JSON 未改，23:29 快速失败并覆盖了 c_lane 原文件；其后 23:56 工具修正、
  占位预注册报告落盘、坏输出被清除（23:26 曾出现的 bt_act12_t4_pit.json 现已消失）。
  任务4 仍在运行，本验收不判其罪，只记录事实与时序。
- **可恢复性（好消息）**: `tmp/c_lane.log`（mtime 8/27 12:42，未被污染）保留
  全部 11 变体的逐窗 ret/base/loss/达标/牛市损数值行，末行 "saved ...json"；
  `docs/reports/c_lane_redispatch.md` 原表格完整；且 C 批配方确定性（seed=42、
  实例级 RNG、钉快照）支持整批重跑复现（8/27 验收时已验证过逐位一致）。
  trades/MDD 两列 log 中缺失，需重跑或从报告正文引用补齐。
- 违反条款: P72"并行协调——改任何共享文件前查 mtime"、通用红线 5 的确定性精神。
- 处置建议见 §7 整改提示词（不自行修复，验收纪律）。

## 6. 评分汇总

| 任务 | 判定 | 分数 | 一句话 |
|---|---|---|---|
| 任务1 服务恢复 | ✅ | 92 | 证据链最规范的一份；乱码 1 处、批准待用户口供确认 |
| 任务2 数据通道 | ✅ | 88 | 根因定性扎实、自愈实证；星期标签错位一天、协调协议未用 |
| 任务3 P77/P73/择时 | ✅ | 90 | 反号 bug 捕获是本批最大增量贡献；P73 留"待执行"尾巴 |
| 任务4 | — | 不评 | 在跑；但 23:29 覆盖事故若坐实为其手段所为，验收时记阻断项 |
| 任务5 ml 侧车 | ✅ | 86 | 异风格但数字全真、环境限制诚实；两处时效性表述过时 |

**批次均分（4 项可验）: 89 / 100，整批签收 ✅；附加两条必须跟进**:
① §5 事故恢复；② 今天 15:10/15:30–18:30 的 8/31 联合观察（任务1/2 的
"重启后首个交易日"验证仍欠收，含 strategy_buy 首笔入链与 09-01 前的快照推进）。

## 7. 整改/续作提示词（投喂用）

### 7a. 立即发给任务4执行方（若其仍在运行；否则并入其验收）

> 紧急插入（验收方发现，须处理并写入你的报告 §合规）：
> ① `data/bt_c_lane_redispatch.json` 于 08-30 23:29:12 被一次运行覆盖为
>   BrokenProcessPool 错误记录（elapsed 61.5s）。若系你的工具首跑 OUT_JSON
>   指向了 c_lane 路径所致，如实承认并停止一切向该路径的输出；
>   核对 `tools/act12_t4_pit.py` 的 OUT_JSON 现值必须是
>   `data/bt_act12_t4_pit.json` 且全程不变；
> ② 你的预注册节（报告 §0）自述"23:02 落盘早于运行"，但工具 mtime 23:56 晚于
>   23:29 的失败运行——在报告 §合规中如实写明时间线（23:02 预注册 → 23:29
>   首跑事故 → 23:56 修正重启），并将预注册判据的最终文本与首跑前版本做一致性
>   声明（判据若有任何变化，只有 23:02 版参与判定）；
> ③ 完成后追加一节：c_lane 事故恢复记录（你不负责重建，但你的运行不得再触碰
>   该文件——每次增量落盘前 assert 目标路径字符串）。

### 7b. C 批数据重建（新的小任务，可发给任一执行 AI）

> 任务：重建 data/bt_c_lane_redispatch.json 至 8/27 原版内容或等效物。
> 两条路线择优：
> ①（首选）重跑 `python tools/c_lane_redispatch.py`（配方确定性: seed=42、
>   钉 08-26 快照、196 口径未变）——原 44 格约 2 小时；完成后与 tmp/c_lane.log
>   （8/27 原始运行日志，逐窗 ret/base/loss 全在）逐格比对，与
>   docs/reports/c_lane_redispatch.md §1/§3 表格逐位一致才算重建成功；
>   不一致则保留新结果 + 在报告加"2026-08-31 重建"节说明差异与原因。
> ②（兜底）若重跑窗口不够：按 c_lane.log 重建 runs 的 total_return 与 judgement
>   全字段，trades/max_drawdown 从报告正文可引用处回填并在 meta 加
>   `reconstructed_from: tmp/c_lane.log, partial: [trades,mdd]` 声明。
> 红线: 重建期间项目根不得再起其他覆盖性写入；重建前先
>   `Copy-Item data\bt_c_lane_redispatch.json tmp\c_lane_corrupted.json` 留证物；
>   docs/backlog.md 增补一行登记本次事故与重建。

### 7c. 今天（8/31 周一）收盘联合观察（任务1+2 欠收项，协议原样执行）

> 15:10 后服务自动更新是唯一变量；15:30–18:30 冻结手动触发。
> 验收判据: update_state.last_day → 2026-08-31、data/snapshots/2026-08-31/
> market.db 出现、quality_alert 当日条目 min5_latest=2026-08-31、
> **首笔 strategy_buy（若有）入链零新增断裂**（pos_cash+单写者的真人身检验，
> audit_chain_diag --verify 前后对比 + 留底）。16:30 台账 tick 与 20:30
> forward_eval 正常推进。若 15:10 更新再次双零 → 即上次失败的 16:01 场景，
> 立即启动任务2 §7 退避建议的实施评估（这次才真需要改 updater）。
> 若交易引擎 9:15 未自动开启或全天无 strategy_buy，pos_cash 生效验证顺延，
> 记入报告"待观察"，不得口头宣告生效。

### 7d. P73 收割（任务3 尾巴，30 分钟内可完成）

> 8/31 收盘若新快照落地（≥3 快照条件已满足两遍），执行
> `python tools/index_timing_snapshot_retest.py --execute`（16 格，约 15 分钟），
> 判据按脚本内预注册原文；产出 data/bt_it_snapshot_retest.json +
> 更新 timestop_index_reverify.md §B（挂起→完成）与 ml_short_label.md L130 状态行。
> 顺带修正 §C.2 的 config mtime 误写（8/26 07:47 → 8/27 07:30:18，
> 注明 8/26 07:47 是 L215 回滚注释日期，两回事）。

---
验收方写入清单: 仅本报告。验收运行命令均为只读（Get-NetTCPConnection /
Get-CimInstance / sqlite ro+immutable / json 解析比对 / grep / py_compile——
未执行任何 --verify 以外的诊断写路径，未运行任何 --verify）。
