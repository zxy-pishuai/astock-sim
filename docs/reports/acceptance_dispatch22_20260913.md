# 验收｜dispatch_20260913 全 22 单逐项验收（验收方 2026-09-13 夜·二轮重验）

**判定：93/100 有条件通过。22 单全部有交付物；20 单通过，A1/B1 判据待交易日实测，
另需你批 3 项动作。**
（轮次说明：一轮验收 90 分，但当时"18 份报告对 22 单"的对账方法有误——文件列表
被截断 + 报告不按单号命名，导致我误判 A2/A4/A5 报告缺失。本表以本轮全量证据为准。）

## 数据面硬证据（我亲测）

| 项 | 实测 | 派单判据 | 判定 |
|---|---|---|---|
| A1 覆盖率 | 09-09~11 各 4995/4988/4993 行；**有效票池口径 4993/5000 ≈ 99.9%** | 连续2日 ≥0.95 | ⏳ 待 09-14/15 实测 |
| A2 amount | 09-09 起 amount=0 **残留 0 行**；一致性抽检 300 样本越界 3（1%） | 各日 ≤20 行 | ✅ |
| A5 指数 | 三指数各 **1918 行，最早 2018-10-22，amount>0=100%，high<low=0** | ≥1800 根/≤2019-01-05/high<low=0/amount≥95% | ✅ 全项超标 |
| B1 快照 | 09-09/10/11 目录齐 + manifest `reconstructed=False, pit_lost=True` | 每日有目录、不伪造 PIT | ✅（合规标注） |
| B4 归档命名 | 实测 `audit_20260910.jsonl` 首条 t=**2026-08-16** → 命名错位确证 | 一致或有明确说明 | ✅ 已确证+映射表待批 |
| D3 面板 | `/api/data_health` **HTTP 200 活体**（我测 1,097ms 冷帧；报告称 91ms/p95≤300ms） | p95 ≤300ms | ⚠️ 我方单次 1.1s，下轮复测 |

## 代码面标记（我 grep 实测）

A1：`BUDGET_MAX=5400` ✅、`PARALLEL_WORKERS=8` 并发 ✅、熔断签名 ✅、批次切片 bug 修复 ✅；
A4：updater 交易日门 15 处 ✅；C1：`TRAILING_ENTRY_GATE` + `position_peak_repaired` ✅；
C5：`strategy_sell` ✅；D1：`quality_alert` 转发 + `escalated` ✅；D4：`trigger_price/slippage_bp` ✅；
B2：`max_age_days` + `data_snapshot_partial` ✅；E3：跑批标记+阈值放宽 ✅；E4：`source_degradation` ✅。

## 逐单结论（报告路径）

- **A1 ✅（诊断到位）** `daily_coverage_fix_20260913.md`——**回答了我此前没拿到的关键问题**：
  兜底 0 进展 = 兜底任务无等锁机制，撞上 run_update 锁直接退出；另修批次切片错位、
  `--close` 等锁重试、补 2568 行 0 失败、单测 10/10。`daily_skip_codes.json` 与
  `stale_first_calib.json` 缺席属**惰性落盘**（无失败/无跑批即无文件），非缺失。
- **A2 ✅** `a2_amount_zero_fix.md`；**A3 ✅** 报告+2 commit+TianjiCloseCatchup 已注册；
  **A4 ✅** `a4_close_update_guard.md`；**A5 ✅** `backfill_index_2019.md`（本轮最漂亮的交付）。
- **B1 ✅/⏳** 分级落盘+防伪标注齐，3 日连续性待观察；**B2 ✅**；**B3 ✅**（73 项/8.59GB
  待删清单，零删除，红线遵守）；**B4 ✅**（疑点①确证、09-11 异常归因=引擎半僵死+无交易）。
- **C1 ✅**；**C2 ✅**（亏损 92.8% 来自 score、A1 影子机会成本 **+30.88pp**）；
  **C3 ✅**（B4 高潮情绪闸门 PASS：IS+0.80/OOS+0.69pp，其余 9 臂 NULL）；
  **C4 ✅**（min5∩A1 仅 0.2% → 改用 limit_pool 代理；**S2 成交率 0.763 / S3 0.504**，
  折扣后 S2 主口径 IS+1.942%/OOS+1.688%——**edge 在真实排队下保留约 3/4**）；
  **C5 ✅**。
- **D1 ✅**；**D2 ✅**（TianjiAuditWatch 已注册）；**D3 ⚠️**（功能通过，延迟待复测）；
  **D4 ✅**。**E1 ✅**（只读对账，零开关改动）；**E2 ✅**（开关保持 False，J1/J2/J3
  判据齐——J1 结论下轮确认）；**E3 ✅**（实测间隔已≥20min，无需再拉；阈值 3→6+跑批标记）；
  **E4 ✅**（降级补偿钩子 + A/B/C 三验通过）。

## 需你批的三项动作

1. **B3 待删清单执行**（73 项 / 8.59GB，含 2 个 1.57GB 的 .bak 与 `_archive_pending_del` 3.79GB）；
2. **B4 历史归档重命名**（仅改名不改内容，映射表已出）；
3. **C2/C3 落地决策**：score 降权 + A1/board 提权 + B4 高潮闸门是否上线（研究单不自行上线，正确）。

## 挂账（下轮亲验）

A1/B1 连续 2~3 交易日判据（09-14~09-16）；D3 冷帧复测；E2 的 J1「R2 判定差异天数」
与 C4 的 S3 保守臂数字；C3 的 B4 PASS 臂判据复刻。另：本轮仅 6 个单号 commit（其余
被 nightly auto 吸收）——报告命名与提交规范建议下轮统一。
