# P71-A2 修复报告：比亚迪 002594 前复权接缝 —— 建议移出 DATA_EXCLUDE_CODES

- 状态：**修复完成，待验收方落地移出**（本工具链未改 `app/config.py`）
- 执行：`tools/repair_002594.py`（复用 `tools/repair_qfq.py` 的 `fetch_raw/make_qfq` 同款流程：mootdx 原始日K + 通达信 xdxr 45 条 + 纯 Python baoli 前复权重建）
- 时间线：2026-08-27 02:12 备份 → 单事务写库 3,671 行（WAL）→ 复检归零；服务全程未动，写前 mtime 守卫静默 90s 确认

## 一、缺陷与根因

- 现象：kline(day) `2025-07-16 C=106.2240 → 2025-07-17 O=326.2140`，**open 跳变 +207.10%**（10% 主板限幅，high 口径）；P54 分流 true_qfq_pollution，随 P71 进入排除清单
- 根因：整段历史为腾讯源分两段写入、两段前复权基准不一致的"跨段接缝"（与 Phase35 发现的 23 只同型），非真实交易跳变。xdxr 存在（45 条），具备可靠重建条件

## 二、执行记录（红线逐项）

| 步骤 | 结果 |
|---|---|
| mtime 守卫 | market.db 静默确认 90s（before `1787739780.80`）后才写 |
| 备份先行 | `data/backups/kline_day_002594_before_byd_repair_20260827_021248.json`（day 全部 **1,855 行**，含 OHLCV+A） |
| 校验闸门 | 2019 起 >21% 残余接缝 = **0**；最新收盘对齐 `2026-08-25 C=91.36 == 库内 91.36`；两者全过才写库（否则中止） |
| 写库 | **单事务** INSERT OR REPLACE 3,671 行（volume×100 口径沿用 repair_qfq；amount 沿用库内原值，缺失才估算） |
| 写后复检 | 全史重扫 >21% 接缝 = **0（归零）**；DB mtime after `1787767968.77` |

## 三、前后对照（关键接缝）

| 日期 | 修复前 O / H / L / C | 修复后 O / H / L / C | 说明 |
|---|---|---|---|
| 2025-07-16 | 106.0040 / 106.9540 / 106.0040 / 106.2240 | 不变 | 接缝前段基准不变 |
| **2025-07-17** | **326.2140 / 329.9479 / 325.3106 / 329.2453 (+207.10%)** | **106.6507 / 107.8907 / 106.3507 / 107.6573 (+0.40%)** | 第二段错误基准整体重锚 |
| 2026-08-25（最新） | C=91.36 | C=91.36（不变） | 近端原始价段不受影响 |

## 四、哨兵口径复检

- 以哨兵 high 判据（|次日开盘/当日收盘−1|>21%）对修复后 002594 全史扫描：**0 处**（修前该票 high 计数 1）
- 声明：本修复只写 `market.db` 的 kline(day) 与备份文件；未动 min5.db / config.py / 其他表

## 五、建议（验收方落地）

1. **将 `"002594"` 移出 `app/config.py` 的 `DATA_EXCLUDE_CODES`**（该行注释现为 `比亚迪 +207.1% 2025-07-16->2025-07-17 P54 真复权污染`）。修复已根治污染源，继续排除只会无谓损失一只高流动性核心票。
2. 快照时效注意：冻结快照 `data/snapshots/2026-08-26/market.db` 先于本修复生成，其中 002594 仍是旧污染序列；回测类验收若涉及该票请以**下一个交易日快照起**为准（updater 收盘更新后自动带出新序列）。实盘行情实时价不受影响。
3. 后续批次提示：本次未写入 `data/qfq_repair_progress.json`（避免"已处理"键污染未来批量语义）；若未来全量 defect 清单再含 002594，重复运行同样幂等安全。
4. 可选复核：与 P35 的 23 只同型接缝批次（2024-12-30 一段式错锚）可用同一驱动批量处理（把 CODE 参数化即可），本轮未扩权。

—— 工具：`tools/repair_002594.py --check/--apply`；产物：备份 json + 本报告；不复权其他表、不重启服务。
